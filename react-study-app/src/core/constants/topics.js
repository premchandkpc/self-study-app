const TOPIC_DEFS = [
  {
    id: 'dsa', label: 'DSA', icon: '\uD83E\uDDE9',
    meta: {
      color: 'blue',
      desc: 'Master fundamental data structures and algorithms with interactive visualization. Step through every operation from array indexing to graph traversal.',
      objectives: [
        'Understand array operations: indexing, searching, sorting in O(n) or O(n log n)',
        'Visualize pointer manipulation in linked lists and trees',
        'Trace graph algorithms: BFS, DFS, Dijkstra, topological sort',
        'Learn dynamic programming: memoization vs tabulation, optimal substructure'
      ],
      keyTopics: ['Time/Space Complexity', 'In-place vs Extra Space', 'Recursion & Stack Frames', 'Graph Connectivity']
    },
    subtopics: [
      { name: 'Arrays', visualizer: 'array', route: '/visualizer/array',
        explanation: `Arrays are contiguous memory blocks storing fixed-size elements. Direct index access is O(1), but insertion/deletion requires shifting elements O(n). Key techniques: sliding window for subarray problems, prefix sums for range queries, two pointers for sorted arrays.`,
        useCases: ['Searching & sorting', 'Dynamic arrays (ArrayList)', 'Cache-friendly sequential access'],
        realWorld: 'YouTube: Video recommendations use arrays to store candidate videos, sorted by relevance score.',
        complexity: { access: 'O(1)', search: 'O(n)', insert: 'O(n)', delete: 'O(n)' } },
      { name: 'TwoPointers', visualizer: 'array', route: '/visualizer/array',
        explanation: `Two pointers approach uses multiple pointers moving through data to solve problems without nested loops. Works on sorted arrays. Examples: finding pairs that sum to target, removing duplicates, container with most water.`,
        useCases: ['Sorted array problems', 'Palindrome checking', 'Merging sorted arrays'],
        realWorld: 'Instagram: Finding overlapping time periods when users were online simultaneously.',
        complexity: { time: 'O(n)', space: 'O(1)' } },
      { name: 'BinarySearch', visualizer: 'array', route: '/visualizer/array',
        explanation: `Divides search space in half each iteration achieving O(log n) search time. Requires sorted data. Critical for: finding elements, counting occurrences, finding boundaries, rotated arrays.`,
        useCases: ['Searching sorted data', 'Finding insertion position', 'Range queries'],
        realWorld: 'WhatsApp: Finding message within billions of messages by timestamp.',
        complexity: { time: 'O(log n)', space: 'O(1)' } },
      { name: 'PrefixSum', visualizer: 'array', route: '/visualizer/array',
        explanation: `Preprocesses array to enable O(1) range sum queries. Build prefix array where prefix[i] = sum of elements [0...i]. Query: sum(l, r) = prefix[r+1] - prefix[l]. Space-time tradeoff: O(n) preprocessing, O(1) queries.`,
        useCases: ['Range sum queries', '2D matrix sums', 'Continuous subarray problems'],
        realWorld: 'YouTube Analytics: Computing total watch time across any date range instantly.',
        complexity: { preprocess: 'O(n)', query: 'O(1)' } },
      { name: 'Matrices', visualizer: 'matrix', route: '/visualizer/matrix',
        explanation: '2D arrays for grid-based problems. Traversal patterns: row-major, column-major, spiral, diagonal. Transpose, rotate, and matrix multiplication.',
        useCases: ['Image processing', 'Game boards', 'Dynamic programming tables'],
        realWorld: 'Instagram: Image filters apply convolution matrices to each pixel.',
        complexity: { access: 'O(1)', traverse: 'O(m\u00D7n)' } },
      { name: 'LinkedList', visualizer: 'linkedlist', route: '/visualizer/linkedlist',
        explanation: `Nodes with data + pointer to next node. Insert/delete at known position is O(1), but access is O(n). No cache locality. Use when: frequent middle insertions, variable size unknown, or when array resizing is expensive.`,
        useCases: ['LRU cache implementation', 'Graph adjacency lists', 'Undo/redo stacks'],
        realWorld: 'Uber: Driver location history as linked list for efficient recent-track lookups.',
        complexity: { access: 'O(n)', insert: 'O(1)', delete: 'O(1)' } },
      { name: 'Graphs', visualizer: 'graph', route: '/visualizer/graph',
        explanation: `Vertices + edges represent relationships. Directed vs undirected, weighted vs unweighted. BFS for shortest path (unweighted), DFS for connectivity/cycles, Dijkstra for shortest path (weighted). Representation: adjacency list vs matrix.`,
        useCases: ['Social networks', 'Route finding', 'Recommendation systems', 'Dependency resolution'],
        realWorld: 'Instagram: Friend graph for feed generation, suggestions using graph traversal.',
        complexity: { bfs: 'O(V+E)', dfs: 'O(V+E)', dijkstra: 'O((V+E) log V)' } },
      { name: 'Trees', visualizer: 'tree', route: '/visualizer/tree',
        explanation: `Acyclic graphs with parent-child hierarchy. BST maintains order: left < parent < right. Height affects operation complexity. Balanced trees (AVL, Red-Black) maintain O(log n) height. Traversals: inorder (sorted), preorder (prefix), postorder (cleanup).`,
        useCases: ['Sorted data with fast insert/delete', 'File systems', 'Database indexes', 'Expression parsing'],
        realWorld: 'YouTube: B-trees in database indexes for video metadata lookups.',
        complexity: { bst: 'O(log n) avg, O(n) worst', balanced: 'O(log n) guaranteed' } },
      { name: 'Trie', visualizer: 'trie', route: '/visualizer/trie',
        explanation: `Tree data structure for efficient string prefix operations. Each node represents a character, root is empty. Insert/Search/Delete in O(m) where m=word length. Prefix search enables autocomplete, spell check, IP routing. More memory than hashmap but faster prefix operations.`,
        useCases: ['Autocomplete', 'Spell checking', 'IP routing (longest prefix match)', 'Dictionary word search'],
        realWorld: 'Google Search: Autocomplete suggestions powered by trie traversal with frequency-based ranking.',
        complexity: { search: 'O(m)', insert: 'O(m)', space: 'O(alphabet \u00D7 nodes)' } },
      { name: 'Union Find', visualizer: 'unionfind', route: '/visualizer/unionfind',
        explanation: `Disjoint Set Union tracks element partitioning. Two operations: find (determine set), union (merge sets). Path compression flattens tree for amortized O(\u03B1(n)). Union by rank keeps tree shallow. Used for connected components, cycle detection, Kruskal MST.`,
        useCases: ['Dynamic connectivity', 'Cycle detection in graphs', 'Kruskal MST algorithm', 'Social network friend groups'],
        realWorld: 'Facebook: Union-Find determines if two users are in same social circle (connected component).',
        complexity: { find: 'O(\u03B1(n))', union: 'O(\u03B1(n))' } },
      { name: 'Backtracking', visualizer: 'backtracking', route: '/visualizer/backtracking',
        explanation: `Brute-force with pruning: build solution incrementally, abandon (backtrack) when constraint violated. Choose \u2192 Explore \u2192 Unchoose pattern. N-Queens: place queens row by row, backtrack if attacked. Sudoku: try digits, backtrack if conflict. Permutations: swap and recurse.`,
        useCases: ['Combinatorial search (N-Queens, Sudoku)', 'Constraint satisfaction', 'Permutations and combinations', 'Maze solving'],
        realWorld: 'Sudoku solvers: Backtracking with constraint propagation solves any valid 9x9 puzzle in milliseconds.',
        complexity: { time: 'O(branches^depth) worst', pruning: 'Greatly reduces in practice' } },
      { name: 'DP', visualizer: 'dp', route: '/visualizer/dp',
        explanation: `Break problem into overlapping subproblems, store results to avoid recomputation. Two approaches: top-down (memoization) with recursion, bottom-up (tabulation) with iteration. Fibonacci, coin change, knapsack, LCS are classic examples.`,
        useCases: ['Optimization problems', 'Longest sequences', 'Cost minimization', 'Path problems'],
        realWorld: 'Uber: Computing cheapest route considering traffic patterns (cost DP).',
        complexity: { space: 'O(subproblems)', time: 'O(subproblems \u00D7 work per problem)' } },
      { name: 'Strings', visualizer: 'string', route: '/visualizer/string',
        explanation: `KMP algorithm finds pattern in text O(n+m). Trie stores strings as tree for fast prefix search. Hashing strings enables O(1) comparison. Rolling hash for substring matching. Palindrome checks: expand-around-center or manacher algorithm.`,
        useCases: ['Pattern matching', 'Autocomplete', 'Spell checking', 'Text editing'],
        realWorld: 'Instagram: Hashtag autocomplete using trie, pattern matching for content filtering.',
        complexity: { kmp: 'O(n+m)', trie: 'O(m) search' } },
      { name: 'HashMaps', visualizer: 'hashmap', route: '/visualizer/hashmap',
        explanation: `Hash function maps keys to indices in O(1) average case. Collisions handled by chaining or open addressing. Load factor determines resizing. Critical for: deduplication, frequency counting, grouping, caching.`,
        useCases: ['Deduplication', 'Counting frequencies', 'Grouping', 'Caching', 'Two-sum problems'],
        realWorld: 'WhatsApp: Hash table for storing user accounts, message deduplication, cache for frequently accessed chats.',
        complexity: { avg: 'O(1)', worst: 'O(n)' } },
      { name: 'Sets', visualizer: 'set', route: '/visualizer/set',
        explanation: 'Unique element collections. Hash-based (unordered, O(1) ops) vs Tree-based (ordered, O(log n) ops). Intersection, union, difference operations.',
        useCases: ['Deduplication', 'Membership testing', 'Removing duplicates'],
        realWorld: 'YouTube: Set of watched video IDs prevents recommending same video twice.',
        complexity: { add: 'O(1) avg', contains: 'O(1) avg' } },
      { name: 'Bubble Sort', visualizer: 'sorting', route: '/visualizer/sorting',
        explanation: `Simple but slow: repeatedly swap adjacent elements if out of order. O(n\u00B2) time always. Optimized: stop if no swaps occur (nearly sorted data). Teaching tool: shows comparison & swap visualizations.`,
        useCases: ['Teaching', 'Nearly sorted data'],
        realWorld: 'Educational visualizations only - never used in production.',
        complexity: { time: 'O(n\u00B2)', space: 'O(1)', stable: 'Yes' } },
      { name: 'Merge Sort', visualizer: 'sorting', route: '/visualizer/sorting',
        explanation: `Divide-and-conquer: split array, recursively sort halves, merge results. O(n log n) guaranteed. Requires O(n) extra space for merging. Stable: equal elements maintain order.`,
        useCases: ['Sorting linked lists', 'External sorting', 'When stable sort needed'],
        realWorld: 'Java Arrays.sort() for objects uses merge sort variant.',
        complexity: { time: 'O(n log n)', space: 'O(n)', stable: 'Yes' } },
      { name: 'Quick Sort', visualizer: 'sorting', route: '/visualizer/sorting',
        explanation: `Divide-and-conquer with partitioning: pick pivot, partition into smaller/larger, recursively sort. O(n log n) average, O(n\u00B2) worst (bad pivot). In-place: O(log n) space for recursion. Cache-friendly.`,
        useCases: ['General sorting', 'Quickselect (find kth element)'],
        realWorld: 'Most popular: Java primitives, C qsort, Python sorted() use variants.',
        complexity: { time: 'O(n log n) avg, O(n\u00B2) worst', space: 'O(log n)', stable: 'No' } },
      { name: 'Heap Sort', visualizer: 'sorting', route: '/visualizer/sorting',
        explanation: `Build max heap, repeatedly extract root (largest). O(n log n) guaranteed, in-place. Not stable. Heap invariant: parent >= children. Selection by extracting k times gives top-k in O(n log k) time.`,
        useCases: ['Top-k selection', 'Priority scheduling', 'Sorting when O(1) space needed'],
        realWorld: 'Linux kernel: Completely Fair Scheduler uses heap for task priority management.',
        complexity: { time: 'O(n log n)', space: 'O(1)', stable: 'No' } },
    ]
  },
  {
    id: 'java', label: 'Java', icon: '\u2615',
    meta: {
      color: 'yellow',
      desc: 'Deep dive into JVM internals, memory management, concurrency. Watch variables on the stack, objects on the heap, garbage collection in action.',
      objectives: [
        'Trace object creation: class loading \u2192 memory allocation \u2192 initialization',
        'Visualize garbage collection: Mark-Sweep, Generational, CMS algorithms',
        'Understand thread lifecycle: new \u2192 runnable \u2192 blocked \u2192 terminated',
        'Debug lock contention with mutex visualization',
        'See how HashMap resizes and how treeification works'
      ],
      keyTopics: ['Heap vs Stack', 'GC Roots & Reachability', 'Lock Ordering', 'Weak References']
    },
    subtopics: [
      { name: 'JVM', visualizer: 'jvm', route: '/visualizer/jvm',
        explanation: `Just-In-Time compilation interprets bytecode initially, then compiles hot code paths to native machine code. Execution happens in stack frames: each method call pushes frame with local variables, operand stack, method reference. Heap stores all objects, managed by garbage collector.`,
        useCases: ['Cross-platform execution', 'Memory management', 'Runtime optimization'],
        realWorld: 'Uber backend written in Java, JVM optimizes ride-matching algorithm execution dynamically.',
        complexity: { compile: 'O(1) threshold-dependent', execution: 'O(1) after JIT' } },
      { name: 'GC', visualizer: 'jvm', route: '/visualizer/jvm',
        explanation: `Garbage Collection automatically frees unused memory. Mark-Sweep: mark reachable objects, sweep unreachable. Generational GC: most objects die young, optimize young generation collection. STW (Stop-The-World) pauses all threads during collection, affecting latency.`,
        useCases: ['Memory management', 'Preventing memory leaks', 'Latency tuning'],
        realWorld: 'YouTube: Tuning GC pause times to keep video streaming latency under 50ms.',
        complexity: { pause: 'Proportional to live objects', throughput: '95%+ achievable' } },
      { name: 'Threads', visualizer: 'threads', route: '/visualizer/threads',
        explanation: `Threads share process memory, enabling concurrent execution. Context switching: OS saves/restores thread state. Synchronization needed for shared data access. Thread states: new \u2192 runnable \u2192 running \u2192 blocked/waiting \u2192 terminated.`,
        useCases: ['Concurrent request handling', 'Background tasks', 'Responsive UI'],
        realWorld: 'WhatsApp: Thread pool handles incoming messages concurrently, each connection on separate thread.',
        complexity: { switch: 'Microseconds', pool: 'O(threads) memory' } },
      { name: 'Locks', visualizer: 'threads', route: '/visualizer/threads',
        explanation: `Mutex (mutual exclusion) ensures only one thread executes critical section. Deadlock: circular waiting for locks. Lock ordering prevents deadlock: always acquire locks in same order. Java ReentrantLock more flexible than synchronized keyword.`,
        useCases: ['Protecting shared state', 'Preventing race conditions', 'Coordinating threads'],
        realWorld: 'Instagram: Locks protect user database updates when multiple services modify same user profile.',
        complexity: { acquire: 'O(1)', contention: 'Microseconds to milliseconds depending' } },
      { name: 'Collections', visualizer: 'java-collections', route: '/visualizer/java-collections',
        explanation: `ArrayList: dynamic array, O(1) append amortized. LinkedList: pointer-based, O(1) insert/delete but O(n) access. HashMap uses hash table, O(1) lookup avg case. ConcurrentHashMap for thread-safe access without full table locks.`,
        useCases: ['Data storage', 'Thread-safe caching', 'Ordered collections'],
        realWorld: 'Uber: ConcurrentHashMap stores active driver locations, updated by thousands of threads simultaneously.',
        complexity: { arraylist: 'O(n) search', hashmap: 'O(1) avg' } },
      { name: 'Streams', visualizer: 'java-streams', route: '/visualizer/java-streams',
        explanation: 'Java Streams enable functional-style operations on collections. Lazy evaluation, intermediate (filter, map) and terminal (collect, reduce) operations. Parallel streams leverage ForkJoinPool.',
        useCases: ['Data processing pipelines', 'Parallel operations', 'Functional programming'],
        realWorld: 'Processing millions of transactions with parallel streams for analytics.',
        complexity: { sequential: 'O(n)', parallel: 'O(n/cores)' } },
      { name: 'Classloader', visualizer: 'jvm', route: '/visualizer/jvm',
        explanation: 'Class loading: load \u2192 link \u2192 initialize. Parent delegation: bootstrap \u2192 extension \u2192 application classloader. Loading: find binary class data. Linking: verify, prepare (allocate static), resolve (optional). Initialization: execute static initializers.',
        useCases: ['Dynamic class loading', 'Plugin systems', 'Custom classloaders'],
        realWorld: 'Tomcat web server uses separate classloader per web app for isolation.',
        complexity: { load: 'O(class size)', lazy: 'Classes loaded on first use' } },
      { name: 'String Pool', visualizer: 'jvm', route: '/visualizer/jvm',
        explanation: 'String literals stored in heap String Pool (interned). == compares reference (pooled strings may share). new String() always creates new object. intern() adds to pool. Saves memory by deduplication.',
        useCases: ['Memory optimization', 'String comparison'],
        realWorld: 'JSON parsing: field names like "name" appear millions of times, pooling saves GBs of heap.',
        complexity: { pool_lookup: 'O(1)', dedup: 'Automatic for literals' } },
      { name: 'HashMap Internals', visualizer: 'jvm', route: '/visualizer/jvm',
        explanation: 'Array of buckets, each bucket is linked list or tree. hash = hashCode() % capacity. Collision: same bucket, chain entries. Threshold: load factor 0.75 triggers resize. Treeify: when bucket > 8 entries, convert to Red-Black tree for O(log n) search.',
        useCases: ['Fast key-value lookup', 'Caching', 'Data grouping'],
        realWorld: 'HashMap<String, User> stores millions of users, O(1) lookup for session management.',
        complexity: { get: 'O(1) avg, O(log n) worst', put: 'O(1) avg, O(n) resize' } },
      { name: 'Volatile', visualizer: 'jvm', route: '/visualizer/jvm',
        explanation: 'volatile ensures visibility across threads: writes happen-before subsequent reads. Prevents CPU cache incoherence. No atomicity: i++ still needs lock. Use for flags, status variables, not counters.',
        useCases: ['Thread visibility guarantees', 'Flag variables', 'State indicators'],
        realWorld: 'Shutdown flag: volatile boolean running = true; thread checks running on each iteration.',
        complexity: { read: '~ same as non-volatile', write: 'Cache coherence overhead' } },
      { name: 'Exception Stack', visualizer: 'jvm', route: '/visualizer/jvm',
        explanation: 'Exception unwinds call stack, filling stack trace. try-catch-finally: finally always executes (even with return). try-with-resources: auto-close AutoCloseable. Checked exceptions must be caught or declared. Runtime exceptions unchecked.',
        useCases: ['Error handling', 'Resource cleanup', 'Control flow for exceptional cases'],
        realWorld: 'Spring: exception thrown in controller, @ExceptionHandler method catches and returns error response.',
        complexity: { throw: 'Stack walk, O(stack depth)', catch: 'O(1) if matched' } },
    ]
  },
  {
    id: 'spring', label: 'Spring', icon: '\uD83C\uDF43',
    meta: {
      color: 'yellow',
      desc: 'Spring Framework architecture: dependency injection, aspect-oriented programming, transaction management, and enterprise patterns.',
      objectives: [
        'Understand Bean lifecycle: instantiation \u2192 property injection \u2192 initialization',
        'See how AOP proxies intercept method calls and apply cross-cutting concerns',
      ],
      keyTopics: ['IoC Container', 'AOP Proxies', 'Transaction Propagation', 'AutoConfiguration']
    },
    subtopics: [
      { name: 'IoC', visualizer: 'spring', route: '/visualizer/spring',
        explanation: 'Inversion of Control: Spring container manages bean lifecycle. Dependency Injection: constructor, setter, field injection. @Component, @Service, @Repository stereotype annotations. @Autowired resolves dependencies. ApplicationContext: loads beans, manages lifecycle.',
        useCases: ['Decoupling dependencies', 'Configuration management', 'Testable components'],
        realWorld: 'Spring Boot auto-configures DataSource, JdbcTemplate, TransactionManager based on classpath dependencies.',
        complexity: { container_start: 'Seconds', bean_init: 'O(dependencies) resolution' } },
      { name: 'MVC', visualizer: 'spring', route: '/visualizer/spring',
        explanation: 'Model-View-Controller: @Controller handles requests, @RequestMapping maps URLs, ViewResolver resolves view. DispatcherServlet: front controller pattern. HandlerMapping: finds controller for URL. HandlerAdapter: invokes controller method.',
        useCases: ['Web application controllers', 'REST API endpoints', 'Request routing'],
        realWorld: 'Instagram: Spring MVC handles millions of API requests per second with async support.',
        complexity: { request: 'O(1) routing', response: 'Depends on view rendering' } },
      { name: 'AOP', visualizer: 'spring', route: '/visualizer/spring',
        explanation: `Aspect-Oriented Programming: separate cross-cutting concerns (logging, security, transactions). @Aspect: defines advice (before, after, around). Pointcut: expression matching method execution. Proxy-based: Spring creates proxy around target object, intercepts calls.`,
        useCases: ['Logging', 'Security checks', 'Transaction management', 'Performance monitoring'],
        realWorld: '@Transactional creates AOP proxy that opens/commits transaction around method execution.',
        complexity: { proxy: 'O(1) per bean', advice: 'Intercepted method call overhead' } },
      { name: 'Transactions', visualizer: 'spring', route: '/visualizer/spring',
        explanation: '@Transactional: declarative transaction management. Propagation: REQUIRED (join existing), REQUIRES_NEW (suspend, create new), NESTED (savepoint). Isolation: READ_COMMITTED, SERIALIZABLE. Rollback: rollback for RuntimeException, not checked exceptions.',
        useCases: ['Database consistency', 'Atomic operations', 'Rollback handling'],
        realWorld: 'Bank transfer: debit and credit in single transaction, any failure rolls back both completely.',
        complexity: { propagation: 'PlatformTransactionManager', isolation: 'Database-specific' } },
      { name: 'Security', visualizer: 'spring', route: '/visualizer/spring',
        explanation: 'Spring Security: authentication & authorization filter chain. SecurityFilterChain: ordered filters (login, logout, CSRF, CORS). AuthenticationProvider: validates credentials. SecurityContext: holds current user auth info (thread-local). @PreAuthorize: method-level security.',
        useCases: ['Authentication', 'Role-based access control', 'API security', 'OAuth2 integration'],
        realWorld: 'Uber: Spring Security validates JWT tokens on every API request, enforces driver/rider role permissions.',
        complexity: { filter: 'O(1) per request', auth: 'Token validation in microseconds' } },
      { name: 'JPA', visualizer: 'spring', route: '/visualizer/spring',
        explanation: 'Java Persistence API: ORM mapping Java objects to database tables. @Entity: maps class to table. @OneToMany, @ManyToOne: relationships. EntityManager: persist, find, merge, remove. Hibernate: most popular JPA implementation. Lazy loading: fetch on access.',
        useCases: ['Object-relational mapping', 'Database persistence', 'Relationship management'],
        realWorld: 'Instagram: User entity with @OneToMany posts, lazy-loaded when accessing user.getPosts().',
        complexity: { find: 'O(1) by PK', query: 'SQL execution time' } },
      { name: 'AutoConfig', visualizer: 'spring', route: '/visualizer/spring',
        explanation: 'Spring Boot auto-configuration: @EnableAutoConfiguration scans auto-configuration classes. @ConditionalOnClass, @ConditionalOnMissingBean enable conditional bean creation. spring.factories lists auto-configuration classes. Override by defining your own bean.',
        useCases: ['Zero-config setup', 'Conditional bean creation', 'Opinionated defaults'],
        realWorld: 'Add spring-boot-starter-data-jpa: auto-configures DataSource, EntityManager, TransactionManager automatically.',
        complexity: { load: 'O(config classes)', conditional: 'Evaluated at startup' } },
      { name: 'Cloud', visualizer: 'spring', route: '/visualizer/spring',
        explanation: 'Spring Cloud: distributed system patterns. Service Discovery (Eureka): services register, clients discover by name. Config Server: externalized configuration. Circuit Breaker (Resilience4j): fail-fast when downstream fails. API Gateway (Spring Cloud Gateway): routing, filtering.',
        useCases: ['Microservice coordination', 'Configuration management', 'Resilience patterns'],
        realWorld: 'Uber microservices: each service registers with Eureka, discovers payment-service via name not IP.',
        complexity: { discovery: 'Heartbeat + registry', config: 'Refreshed at runtime' } },
    ]
  },
  {
    id: 'golang', label: 'Go', icon: '\uD83D\uDC39',
    meta: {
      color: 'blue',
      desc: 'Go language: goroutines, channels, scheduler internals. Understand concurrency model, CSP (Communicating Sequential Processes).',
      objectives: [
        'Understand goroutine scheduling on OS threads',
        'Learn channel-based communication patterns',
        'Trace GMP model: goroutine, machine, processor',
      ],
      keyTopics: ['CSP Model', 'GMP Scheduler', 'Channel Types', 'Select Statement']
    },
    subtopics: [
      { name: 'Goroutines', visualizer: 'golang', route: '/visualizer/golang',
        explanation: 'Lightweight concurrent execution: goroutine is user-space thread (stack starts at 2KB). Managed by Go runtime scheduler, not OS. M:N scheduling: M goroutines multiplexed onto N OS threads. go func() starts goroutine. Non-blocking: thousands to millions of goroutines feasible.',
        useCases: ['Concurrent processing', 'Background tasks', 'Parallel computation'],
        realWorld: 'YouTube: Each incoming video upload processed in separate goroutine, 10K+ concurrent uploads handled.',
        complexity: { create: '~100ns', stack: '2KB initial, grows as needed' } },
      { name: 'Channels', visualizer: 'golang', route: '/visualizer/golang',
        explanation: 'CSP communication: goroutines communicate via channels, not shared memory. ch := make(chan int) creates channel. ch <- val sends, val := <-ch receives. Buffered vs unbuffered: unbuffered blocks until both sides ready. Select: multiplex multiple channel operations.',
        useCases: ['Goroutine communication', 'Synchronization', 'Pipeline patterns'],
        realWorld: 'Kubernetes: Go channels coordinate API server, scheduler, controller-manager components.',
        complexity: { send: 'O(1) amortized', receive: 'O(1)', select: 'O(n) channels' } },
      { name: 'Scheduler', visualizer: 'golang', route: '/visualizer/golang',
        explanation: 'GMP model: G=goroutine, M=OS thread, P=processor (logical CPU). P has local run queue. Work stealing: idle P steals G from other P\'s queue. Preemption: goroutine preempted after ~10ms (non-cooperative before Go 1.14). Syscalls: M blocks, P runs other goroutines.',
        useCases: ['Concurrent execution', 'CPU-bound tasks', 'I/O-bound tasks'],
        realWorld: 'Go scheduler handles 100K+ goroutines efficiently: OS thread pool + work stealing.',
        complexity: { schedule: 'O(1) per goroutine', steal: 'O(local_queue)' } },
      { name: 'gRPC', visualizer: 'golang', route: '/visualizer/golang',
        explanation: 'gRPC: high-performance RPC framework using Protocol Buffers. HTTP/2 multiplexes multiple RPCs on single connection. Streaming: unary, server-streaming, client-streaming, bidirectional. Protobuf serialization smaller + faster than JSON.',
        useCases: ['Microservice communication', 'Real-time streaming', 'High-performance APIs'],
        realWorld: 'Uber: gRPC for inter-service communication, 10x faster than JSON HTTP REST.',
        complexity: { serialization: 'Nanoseconds', streaming: 'HTTP/2 frames' } },
    ]
  },
  {
    id: 'python', label: 'Python', icon: '\uD83D\uDC0D',
    meta: {
      color: 'blue',
      desc: 'Python ecosystem: async programming, GIL internals, decorators, and ML pipelines.',
      objectives: [
        'Understand GIL impact on multi-threaded performance',
        'Learn asyncio event loop and coroutine execution',
        'Master decorator patterns and closure behavior',
      ],
      keyTopics: ['GIL', 'Event Loop', 'Coroutines', 'Closures & Decorators']
    },
    subtopics: [
      { name: 'Async', visualizer: 'python', route: '/visualizer/python',
        explanation: 'asyncio: event loop runs coroutines cooperatively. async def defines coroutine, await suspends until result ready. Event loop: schedule, execute, suspend coroutines. Tasks: wrap coroutines for concurrent execution. Non-blocking I/O: single thread handles many connections.',
        useCases: ['I/O-bound applications', 'Web servers', 'Network clients'],
        realWorld: 'YouTube: asyncio handles 100K+ concurrent connections for live chat.',
        complexity: { switch: 'Microseconds (cooperative)', concurrency: '10K+ connections' } },
      { name: 'GIL', visualizer: 'python', route: '/visualizer/python',
        explanation: 'Global Interpreter Lock: only one thread executes Python bytecode at a time. Lock acquired per bytecode (or per ~100 ticks). CPU-bound threads: GIL serializes, no speedup. I/O-bound: GIL released during blocking calls, threads interleave. Solutions: multiprocessing, C extensions, asyncio.',
        useCases: ['Thread safety (free)', 'CPU-bound bottleneck', 'C extension bypass'],
        realWorld: 'NumPy: releases GIL during array operations, enables multi-threaded speedup.',
        complexity: { contention: '5-20% overhead for CPU-bound', I_O: 'Released during blocking' } },
      { name: 'Decorators', visualizer: 'python', route: '/visualizer/python',
        explanation: 'Functions returning functions: @decorator syntactic sugar for func = decorator(func). Closure: inner function captures outer variables. functools.wraps preserves metadata. Class-based decorators with __call__. Stacking: @a @b applies bottom-up.',
        useCases: ['Logging', 'Timing', 'Authentication', 'Memoization'],
        realWorld: 'Flask: @app.route("/") registers function as URL handler via decorator.',
        complexity: { call: 'Decorator overhead per invocation', stack: 'O(n) for nested decorators' } },
      { name: 'ML Pipelines', visualizer: 'python', route: '/visualizer/python',
        explanation: 'Scikit-learn Pipeline: chain transforms + estimator. fit/transform flow through stages. ColumnTransformer: apply different transforms to different columns. GridSearchCV: hyperparameter tuning over pipeline params. Preprocessing: scaling, encoding, imputation.',
        useCases: ['Machine learning workflows', 'Feature engineering', 'Model evaluation'],
        realWorld: 'YouTube recommendation: pipeline encodes video features, scales, feeds to gradient boosting model.',
        complexity: { fit: 'O(epochs \u00D7 data)', transform: 'O(data) per stage' } },
    ]
  },
  {
    id: 'kubernetes', label: 'Kubernetes', icon: '\u2638\uFE0F',
    meta: {
      color: 'blue',
      desc: 'Container orchestration platform automates deployment, scaling, and management. Pods are smallest unit, Services provide networking, Controllers ensure desired state.',
      objectives: [
        'Understand Pod lifecycle: pending \u2192 running \u2192 succeeded/failed',
        'Learn Service types: ClusterIP, NodePort, LoadBalancer',
        'Trace rolling update: gradual pod replacement with zero downtime',
        'Understand HPA: metrics-based autoscaling with exponential backoff',
        'Debug node failure: pod rescheduling on healthy nodes',
      ],
      keyTopics: ['Desired State', 'Control Loop', 'etcd Consensus', 'CNI Networking']
    },
    subtopics: [
      { name: 'Pods', visualizer: 'kubernetes', route: '/visualizer/kubernetes',
        explanation: `Smallest deployable unit containing one or more containers sharing network namespace (same IP). Sidecar pattern: main app + supporting container (logging, metrics). Init containers run before app containers. Pod lifecycle: pending \u2192 running \u2192 succeeded/failed.`,
        useCases: ['Containerized apps', 'Sidecar logging', 'Tightly-coupled services'],
        realWorld: 'YouTube: Each pod runs video transcoding container + monitoring sidecar.',
        complexity: { startup: '100-500ms', scaling: 'Per pod instance' } },
      { name: 'Deployments', visualizer: 'kubernetes', route: '/visualizer/kubernetes',
        explanation: `Manages replica pods, rolling updates, and rollbacks. Desired state: "I want 3 replicas of this image". Rolling update: gradually replace old pods with new ones, maintaining availability. Canary: test new version on subset before full rollout.`,
        useCases: ['Stateless app deployment', 'Zero-downtime updates', 'Scaling', 'Rollback capability'],
        realWorld: 'Instagram: Deploy new feed algorithm to 10% of users first (canary), monitor metrics, then 100%.',
        complexity: { update: 'O(replicas) time', downtime: 'Zero with proper config' } },
      { name: 'Services', visualizer: 'kubernetes', route: '/visualizer/kubernetes',
        explanation: `Stable networking abstraction over ephemeral pods. ClusterIP: internal only. NodePort: expose on node IP. LoadBalancer: cloud provider LB. Service discovery: DNS automatically resolves service name to pod IPs, updating as pods scale.`,
        useCases: ['Pod networking', 'Load balancing', 'Service discovery', 'Exposing apps'],
        realWorld: 'Uber: "driver-location-service" DNS resolves to all driver-tracking pods automatically.',
        complexity: { discovery: 'O(1) DNS', routing: 'O(1) kube-proxy' } },
      { name: 'Ingress', visualizer: 'kubernetes', route: '/visualizer/kubernetes',
        explanation: 'Ingress exposes HTTP/HTTPS routes to Services. Host-based routing (api.example.com), path-based (example.com/api/*). TLS termination at ingress. Ingress Controller: Nginx, Traefik, AWS ALB implement ingress rules. Annotations configure controller-specific features.',
        useCases: ['HTTP routing', 'TLS termination', 'Name-based virtual hosting'],
        realWorld: 'Uber: rider.example.com \u2192 rider-service, driver.example.com \u2192 driver-service via single Ingress.',
        complexity: { routing: 'O(hosts + paths)', tls: 'Terminated at ingress' } },
      { name: 'HPA', visualizer: 'kubernetes', route: '/visualizer/kubernetes',
        explanation: `Horizontal Pod Autoscaler monitors metrics (CPU, memory, custom) and adjusts replica count. Scales up when load high, scales down when low. Works with Metrics Server for metric collection. Min/max replicas prevent thrashing.`,
        useCases: ['Automatic scaling', 'Cost optimization', 'Handling traffic spikes'],
        realWorld: 'YouTube: HPA scales video upload processor pods 10x during evening peak hours, scales down at night.',
        complexity: { latency: '30-60 seconds', scaling: 'Exponential backoff' } },
      { name: 'Config & Storage', visualizer: 'kubernetes', route: '/visualizer/kubernetes',
        explanation: 'ConfigMap: store non-sensitive config (key-value). Secret: base64-encoded sensitive data. Volume: mount storage into pod (emptyDir, hostPath, PVC). PersistentVolume: cluster storage resource. PVC: storage request. StorageClass: dynamic provisioning.',
        useCases: ['Configuration management', 'Secret storage', 'Persistent storage'],
        realWorld: 'Uber: ConfigMap stores service configuration, Secret stores DB credentials, PVC stores ride history.',
        complexity: { configmap: 'O(keys)', volume: 'Mounted at pod start' } },
      { name: 'DNS', visualizer: 'kubernetes', route: '/visualizer/kubernetes',
        explanation: 'CoreDNS: cluster DNS provider. Service DNS: name.namespace.svc.cluster.local. Pod DNS: pod-ip.namespace.pod.cluster.local. Cluster-first: DNS queries go to CoreDNS first, external forwarded to upstream DNS.',
        useCases: ['Service discovery', 'Pod resolution', 'Cluster networking'],
        realWorld: 'Payment service connects to "postgres.default.svc.cluster.local:5432" for database.',
        complexity: { resolution: 'O(1) cached', ttl: '30 seconds default' } },
      { name: 'Sidecars', visualizer: 'kubernetes', route: '/visualizer/kubernetes',
        explanation: 'Sidecar pattern: additional container in same pod providing supporting functionality. Service mesh: Envoy sidecar intercepts all traffic, handles mTLS, retries, metrics. Logging sidecar: ships logs to centralized system. No code changes needed for main app.',
        useCases: ['Service mesh (istio/linkerd)', 'Log shipping', 'Monitoring agents'],
        realWorld: 'Instagram: Envoy sidecar in each pod handles mTLS, retries, and distributed tracing automatically.',
        complexity: { proxy: '~5ms added latency', overhead: '10-50MB memory per sidecar' } },
      { name: 'etcd', visualizer: 'kubernetes', route: '/visualizer/kubernetes',
        explanation: 'Distributed key-value store using Raft consensus. Stores cluster state: pods, services, configmaps, secrets. Watch API: components watch for changes (immediate notification). Quorum: majority (N/2+1) nodes required. Leader handles writes, followers replicate.',
        useCases: ['Cluster state storage', 'Configuration management', 'Service discovery'],
        realWorld: 'Kubernetes API server: all cluster state in etcd, controllers watch for changes and reconcile.',
        complexity: { write: 'RTT to quorum', watch: 'Immediate notification', read: 'Local to each node' } },
      { name: 'Node Failure', visualizer: 'kubernetes', route: '/visualizer/kubernetes',
        explanation: 'Node controller: watches node health via heartbeat. Node not ready: 5 min grace period before pod eviction. Pods on failed node become Terminating, rescheduled on healthy nodes. Unschedulable taint prevents new pods. DaemonSet pods skip reschedule (tied to node).',
        useCases: ['Fault tolerance', 'Self-healing', 'High availability'],
        realWorld: 'AWS AZ failure: pods on failed nodes automatically rescheduled on healthy nodes within 5 minutes.',
        complexity: { detection: '~40 seconds', reschedule: 'Seconds to minutes' } },
    ]
  },
  {
    id: 'aws', label: 'AWS', icon: '\u2601\uFE0F',
    meta: {
      color: 'orange',
      desc: 'Cloud platform for building scalable systems. EC2 for compute, S3 for storage, RDS for databases, Lambda for serverless. Pay-as-you-go model enables rapid scaling.',
      objectives: [
        'Understand EC2 instance types and pricing models',
        'Learn S3 storage classes and lifecycle policies',
        'Trace Lambda cold starts and execution environment reuse',
      ],
      keyTopics: ['Shared Responsibility Model', 'Auto Scaling', 'VPC Networking', 'IAM Policies']
    },
    subtopics: [
      { name: 'EC2', visualizer: 'aws', route: '/visualizer/aws',
        explanation: 'Virtual servers in cloud. Instance types: general purpose (t3), compute (c5), memory (r5), GPU (p3). Pricing: on-demand (pay per hour), reserved (1-3yr discount), spot (up to 90% off, can be terminated). Auto Scaling: maintain desired instance count. AMI: pre-configured instance template.',
        useCases: ['General compute', 'Web servers', 'Batch processing'],
        realWorld: 'Uber: EC2 Auto Scaling group handles ride-matching compute, scales based on request count.',
        complexity: { start: '30-60 seconds', billing: 'Per second' } },
      { name: 'Lambda', visualizer: 'aws', route: '/visualizer/aws',
        explanation: `Serverless compute: write function, deploy, AWS handles scaling/servers. Cold start: first invocation slow (initialization), subsequent warm starts fast. Billed per 100ms execution time. Perfect for event-driven, variable-load workloads.`,
        useCases: ['Image processing', 'API backends', 'Scheduled tasks', 'Event processing'],
        realWorld: 'YouTube: Process video uploads with Lambda, scales from 0 to 10K concurrent automatically.',
        complexity: { cold_start: '100-1000ms', warm: '10ms', cost: 'Pay per 100ms' } },
      { name: 'SQS', visualizer: 'aws', route: '/visualizer/aws',
        explanation: 'Fully managed message queue. Standard: at-least-once, best-effort ordering. FIFO: exactly-once, strict ordering. Visibility timeout: message hidden after read, reappears if not deleted. Dead-letter queue: messages that failed processing. Polling: short (immediate) vs long (wait up to 20s).',
        useCases: ['Decoupling microservices', 'Work queues', 'Buffering'],
        realWorld: 'Uber: Ride request SQS queue buffers requests during peak, services process at their pace.',
        complexity: { send: 'Milliseconds', receive: 'Long poll reduces cost' } },
      { name: 'SNS', visualizer: 'aws', route: '/visualizer/aws',
        explanation: 'Pub/sub messaging: topics have multiple subscribers (SQS, Lambda, HTTP, email). Fan-out: one message to all subscribers. Filter policies: subscriber receives subset of messages. FIFO topics: strict ordering + deduplication.',
        useCases: ['Event notifications', 'Broadcasting', 'Alerting'],
        realWorld: 'Instagram: New post published \u2192 SNS notifies followers\u2019 feed service, notification service, analytics.',
        complexity: { publish: 'Milliseconds', fanout: 'O(subscribers)' } },
      { name: 'EKS', visualizer: 'aws', route: '/visualizer/aws',
        explanation: 'Managed Kubernetes: AWS handles control plane (etcd, API server). Worker nodes: EC2 instances in Auto Scaling groups. Fargate: serverless containers, no node management. IAM roles for service accounts: pod-level IAM permissions.',
        useCases: ['Kubernetes on AWS', 'Container orchestration', 'Hybrid deployments'],
        realWorld: 'Snapchat: EKS runs ML inference workloads, automatically scales based on GPU demand.',
        complexity: { control_plane: 'AWS managed', worker: 'ASG scaling minutes' } },
      { name: 'API Gateway', visualizer: 'aws', route: '/visualizer/aws',
        explanation: 'Managed API frontend: REST, HTTP, WebSocket APIs. Request throttling: per-client rate limiting. Authorization: IAM, Cognito, Lambda authorizer. Caching: response caching reduces backend load. Usage plans: tier-based API access for different customers.',
        useCases: ['API management', 'Throttling', 'Authentication', 'Versioning'],
        realWorld: 'Uber: API Gateway routes to Lambda for simple requests, EC2 for complex matching logic.',
        complexity: { latency: '<10ms added', caching: 'TTL-based' } },
      { name: 'S3', visualizer: 'aws', route: '/visualizer/aws',
        explanation: `Object storage: store any file (photos, videos, documents). Unlimited storage, pay per GB stored. Versioning: keep multiple versions. CloudFront CDN delivers content from edge locations worldwide.`,
        useCases: ['File storage', 'Static hosting', 'Data backup', 'Media distribution'],
        realWorld: 'Instagram: User photos stored in S3, served via CloudFront (cached at 200+ edge locations).',
        complexity: { latency: '10-100ms', availability: '99.99%' } },
      { name: 'IAM', visualizer: 'aws', route: '/visualizer/aws',
        explanation: 'Identity and Access Management: Users (person), Groups (collection), Roles (for services). Policies: JSON documents defining permissions (Allow/Deny). Least privilege: grant minimum permissions needed. Trust policy: who can assume role. Permission boundary: maximum permissions.',
        useCases: ['Access control', 'Service authorization', 'Multi-account management'],
        realWorld: 'EC2 instance gets IAM role, automatically receives temporary credentials for S3 access via instance metadata.',
        complexity: { auth: 'Milliseconds', propagation: 'Eventual consistency' } },
      { name: 'Route53', visualizer: 'aws', route: '/visualizer/aws',
        explanation: 'DNS service: domain registration, routing, health checking. Routing policies: simple, weighted, latency-based, geolocation, failover. Alias records: route to AWS resources (ELB, CloudFront). Health checks: monitor endpoint, failover on failure. 100% SLA.',
        useCases: ['Domain management', 'DNS routing', 'Health checks', 'Failover'],
        realWorld: 'Uber: latency-based routing directs users to nearest region, failover route53 switches if region goes down.',
        complexity: { dns: 'Milliseconds cached', failover: 'Seconds' } },
      { name: 'ECS', visualizer: 'aws', route: '/visualizer/aws',
        explanation: 'Managed container service (Docker). Fargate: serverless containers (no EC2 management). EC2 launch: control over instances. Task definition: container spec (image, port, CPU, memory). Service: maintain desired task count, integrate with ALB. ECR: container image registry.',
        useCases: ['Container deployment', 'Microservices', 'Batch jobs'],
        realWorld: 'Amazon: ECS runs core e-commerce services, Fargate for variable-load tasks.',
        complexity: { fargate: 'No infrastructure management', ec2: 'More control, more overhead' } },
      { name: 'Step Functions', visualizer: 'aws', route: '/visualizer/aws',
        explanation: 'Visual workflow: state machine with tasks, choices, parallelism, waits. Amazon States Language (ASL): JSON-based workflow definition. Execution: each workflow run tracked. Error handling: retry, catch, fallback states. Service integration: Lambda, SQS, SNS, ECS, DynamoDB.',
        useCases: ['Orchestration', 'Multi-step processes', 'Error handling workflows'],
        realWorld: 'Instagram: User registration workflow: create profile \u2192 send welcome email \u2192 initialize storage \u2192 onboarding.',
        complexity: { state_transition: '~100ms', execution: 'Up to 1 year' } },
      { name: 'Aurora / RDS', visualizer: 'aws', route: '/visualizer/aws',
        explanation: `Managed relational database (PostgreSQL, MySQL, Aurora). Handles backups, patches, replication. Read replicas for read scaling. Multi-AZ for high availability: synchronous replication to standby, auto-failover on primary failure.`,
        useCases: ['Transactional data', 'Complex queries', 'ACID guarantees'],
        realWorld: 'Uber: Orders (rides) stored in RDS with read replicas for queries, multi-AZ for resilience.',
        complexity: { read_replica: 'O(1) reads', failover: '30-60 seconds' } },
      { name: 'ElastiCache', visualizer: 'aws', route: '/visualizer/aws',
        explanation: 'Managed Redis/Memcached. Redis: rich data structures, persistence, clustering. Memcached: simple, multi-threaded. Use cases: session store, cache, rate limiting. Cluster mode: shard data across nodes. Replication: primary-replica for high availability.',
        useCases: ['Caching', 'Session storage', 'Real-time analytics'],
        realWorld: 'Uber: Redis caches driver locations (expiring keys), provides sub-millisecond read latency.',
        complexity: { get: '<1ms', set: '<1ms', cluster: 'Hash-slot based' } },
    ]
  },
  {
    id: 'kafka', label: 'Kafka', icon: '\uD83D\uDCE8',
    meta: {
      color: 'red',
      desc: 'Distributed event streaming platform. Brokers store partitioned topics. Producers publish messages, consumers subscribe. Strong ordering per partition, distributed replication for fault tolerance.',
      objectives: [
        'Understand topic partitioning and parallel consumption',
        'Trace consumer group rebalancing on join/leave',
        'Learn ISR (In-Sync Replicas) and acknowledgment guarantees',
        'Monitor consumer lag for processing bottlenecks',
      ],
      keyTopics: ['Log Compaction', 'Partition Leader Election', 'Consumer Offset', 'Exactly-Once Semantics']
    },
    subtopics: [
      { name: 'Partitions', visualizer: 'kafka', route: '/visualizer/kafka',
        explanation: `Topics divided into independent partitions, each stored on different brokers. Producer chooses partition via key hash or round-robin. Consumer reads partition linearly maintaining order. Scaling: more partitions = more parallelism but more coordination overhead.`,
        useCases: ['Parallel processing', 'Ordered event streams', 'Distributed queuing'],
        realWorld: 'WhatsApp: Message topic with 100 partitions, messages from user hashed to same partition maintaining conversation order.',
        complexity: { partition: 'O(1) produce', parallelism: 'O(partitions)' } },
      { name: 'ISR', visualizer: 'kafka', route: '/visualizer/kafka',
        explanation: `In-Sync Replicas: set of replicas that have caught up with leader. Producer waits for ISR acknowledgment before considering message committed. Min ISR guarantees: if min.insync.replicas=2, need 2 in-sync before ACK. Leader failure: new leader chosen from ISR.`,
        useCases: ['Durability guarantees', 'Fault tolerance', 'Preventing data loss'],
        realWorld: 'Uber: Trip events need ISR=2 before confirming ride booking to guarantee no lost payment data.',
        complexity: { commit: 'Network latency \u00D7 ISR size', failover: 'Milliseconds' } },
      { name: 'Consumer Groups', visualizer: 'kafka', route: '/visualizer/kafka',
        explanation: `Multiple consumers subscribe to same topic, each gets subset of partitions. Group coordinates: only one consumer per partition. Rebalancing: when consumer joins/leaves, partitions redistributed. Offset: tracks message position, enables restart from last processed.`,
        useCases: ['Scaling consumers', 'Multiple services subscribing same topic', 'Fault tolerance'],
        realWorld: 'Instagram: Feed generation service has 5 consumers in group, each handles 20 partitions of post topic.',
        complexity: { rebalance: 'Seconds', offset: 'O(1) lookup' } },
      { name: 'Lag', visualizer: 'kafka', route: '/visualizer/kafka',
        explanation: `Consumer lag: difference between latest offset in partition and consumer's committed offset. High lag indicates slow consumers or backed-up producers. Monitoring lag detects processing issues. Dead Letter Queue for messages that fail repeatedly.`,
        useCases: ['Performance monitoring', 'Debugging slowness', 'Alert thresholds'],
        realWorld: 'YouTube: Monitor lag of notification consumer, if lag > 1M messages, page on-call engineer.',
        complexity: { monitor: 'O(partitions)', alert: 'Real-time' } },
    ]
  },
  {
    id: 'system-design', label: 'System Design', icon: '\uD83C\uDFD7\uFE0F',
    meta: {
      color: 'yellow',
      desc: 'Design scalable systems supporting millions of users. Uber ride-hailing demonstrates: distributed payments, real-time matching, geospatial queries, message queuing.',
      objectives: [
        'Learn to decompose systems into manageable services',
        'Understand CAP trade-offs in distributed data stores',
        'Practice designing for scale: caching, sharding, CDNs',
        'Real-world case studies: Uber, WhatsApp, Instagram, YouTube',
      ],
      keyTopics: ['CAP Theorem', 'Sharding Strategies', 'Caching Layers', 'Message Queues']
    },
    subtopics: [
      { name: 'Uber', visualizer: 'uber', route: '/topics/system-design/Uber/learn', scenarioId: 'uber',
        explanation: 'Uber ride-hailing system design covering end-to-end request flow, service architecture, and failure mode analysis. Technologies: gRPC, Kafka, Redis GEORADIUS, PostgreSQL sharding, WebSocket.',
        useCases: ['System design learning', 'Architecture understanding', 'Resilience engineering'],
        realWorld: 'Uber handles 15M+ trips daily across 70+ countries with 2200+ microservices.',
        complexity: { matching: 'O(nearby drivers)', services: '2200+', p99_latency: '50ms' },
        tabs: [
          { name: 'Request Flow',
            explanation: 'End-to-end request flow for Uber ride-hailing: rider request \u2192 matching engine \u2192 driver assignment \u2192 ride tracking \u2192 payment. Each step involves different services communicating via synchronous (gRPC) and asynchronous (Kafka) patterns.',
            useCases: ['System design learning', 'Request tracing', 'Architecture understanding'],
            realWorld: 'Uber handles 15M+ trips daily across 70+ countries.',
            complexity: { matching: 'O(nearby drivers)', payment: 'Distributed transaction' } },
          { name: 'Architecture',
            explanation: 'Microservice architecture: API Gateway, Rider Service, Driver Service, Matching Engine, Pricing Service, Payment Service, Trip Service, Notification Service. Each service independently deployable, owns its data store. Kafka for async communication.',
            useCases: ['System design patterns', 'Service decomposition', 'Architecture planning'],
            realWorld: 'Uber: 2200+ microservices, each owned by separate team, deployed independently.',
            complexity: { services: '2200+', coordination: 'Service mesh + Kafka' } },
          { name: 'Failure Modes',
            explanation: 'Failure scenarios: payment service down (circuit breaker + charge later), matching engine overload (queue requests), database replica failure (failover to standby), Kafka broker failure (ISR elects new leader). Graceful degradation patterns used throughout.',
            useCases: ['Resilience engineering', 'Fault tolerance', 'Graceful degradation'],
            realWorld: 'Uber: Payment circuit breaker prevents cascading failure during payment service outage in 2022.',
            complexity: { failover: 'Seconds', recovery: 'Minutes' } },
        ] },
      { name: 'WhatsApp', visualizer: 'systemdesign', route: '/topics/system-design/WhatsApp/learn',
        explanation: 'Real-time messaging at global scale: 2B+ users, 100B msgs/day, E2EE by default. Architecture, storage, encryption, and delivery mechanics for the world\u2019s largest messaging platform.',
        useCases: ['Real-time messaging design', 'Global scale systems', 'E2EE protocols'],
        realWorld: 'WhatsApp: 500+ chat servers, 1.5B persistent WebSocket connections, acquired by Facebook for $19B in 2014.',
        complexity: { delivery_p50: '35ms', delivery_p99: '450ms', connections_per_server: '500K' },
        tabs: [
          { name: 'Architecture',
            explanation: 'Client → Anycast DNS → Edge LB → WebSocket Gateway (500K conns/node) → Chat Service → Cassandra (write-optimized, multi-DC) + Kafka (async delivery) + Redis (presence, sessions). E2EE via Signal Protocol (X3DH + Double Ratchet). Offline delivery via FCM/APNS push + cursor-based sync on reconnect.',
            useCases: ['Real-time messaging design', 'Persistent connection architecture', 'Global scale systems'],
            realWorld: 'WhatsApp: 500+ chat servers globally, Erlang/Go runtime, Cassandra for messages, PostgreSQL for user data. Acquired by Facebook for $19B in 2014.',
            complexity: { delivery_p50: '35ms', delivery_p99: '450ms', connections_per_server: '500K' } },
          { name: 'Data Store',
            explanation: 'Hybrid storage: Cassandra for messages (write-optimized, time-bucketed partitions yyyy-MM-dd-HH, tunable consistency Write ONE / Read QUORUM). PostgreSQL for users/contacts/groups (sharded by hash(phone) % 1000, LWT for consistency). Redis for ephemeral state (presence TTL 120s, session cache, rate limit counters, dedup keys). S3/Ceph for media blobs.',
            useCases: ['Multi-database architecture', 'Time-bucketed partitioning', 'Ephemeral vs persistent state'],
            realWorld: 'Cassandra cluster: 80+ nodes across multiple DCs. Each message written to 3 tables: sender outbox, receiver inbox, conversation view. PostgreSQL: 1000+ application-level shards.',
            complexity: { cassandra_latency: '5-15ms', redis_latency: '<1ms', replication_factor: '3' } },
          { name: 'E2EE',
            explanation: 'Signal Protocol: X3DH key agreement on first contact (Identity Key + Signed Prekey + Ephemeral Key + One-time Prekey → Shared Secret via KDF). Double Ratchet per message (Root Key → Chain Keys → Message Keys, ratchet step rotates keys). Perfect Forward Secrecy: long-term key compromise ≠ past messages. Server is dumb key directory — stores encrypted prekey bundles, never sees plaintext.',
            useCases: ['End-to-end encryption design', 'Key agreement protocols', 'Perfect forward secrecy'],
            realWorld: 'WhatsApp was first major messaging app to deploy E2EE by default for all content (text, media, calls). Signal Protocol designed by Open Whisper Systems.',
            complexity: { key_exchange: 'X3DH + 4 DH operations', ratchet: 'O(1) per message', pfs: 'Guaranteed' } },
          { name: 'Delivery',
            explanation: 'At-least-once delivery: client generates UUID v7 msg_id, server deduplicates via Redis SETNX (24h TTL). Online: push via WebSocket in <100ms. Offline: Kafka queue (48h retention) + FCM/APNS push notification (coalesced: 10 msgs → 1 push). On reconnect: cursor-based sync fetches missed messages since last_seen_msg_id. Delivery receipts: sent ✓ → delivered ✓✓ (blue) → read ✓✓ (blue, double check).',
            useCases: ['Reliable message delivery', 'Offline message queueing', 'Read/delivery receipts'],
            realWorld: '100B+ messages/day with <100ms P50 delivery latency online. Push notifications wake 1B+ devices daily. Credit-based backpressure prevents OOM on 500K connections/server.',
            complexity: { online_delivery: '<100ms', offline_queue: '48h Kafka', dedup: '24h Redis TTL' } },
        ] },
      { name: 'Load Balancer', visualizer: 'systemdesign', route: '/topics/system-design/Load Balancer/learn', scenarioId: 'lb',
        explanation: `Distributes traffic across servers: round-robin (rotate), least-connections (fewer current), weighted, IP-hash (same user\u2192same server). Health checks: periodically test servers, remove unhealthy ones. Sticky sessions: session affinity.`,
        useCases: ['Scaling web servers', 'High availability', 'Request distribution'],
        realWorld: 'Uber: Load balancer distributes API requests across 1000 backend servers, removes unresponsive ones.',
        complexity: { latency: '<1ms', failover: 'Seconds after health check failure' } },
      { name: 'Cache', visualizer: 'systemdesign', route: '/topics/system-design/Cache/learn', scenarioId: 'cache',
        explanation: 'Store frequently accessed data in fast memory (Redis/Memcached). Cache-aside: app checks cache first, loads on miss. Write-through: write to both cache + DB. Write-behind: write to cache, async write to DB. Eviction: LRU (least recently used), TTL (time-to-live). Cache stampede: thundering herd on cache miss.',
        useCases: ['Read-heavy workloads', 'Database load reduction', 'Low-latency responses'],
        realWorld: 'Instagram: Feed served from Redis cache (millions of reads/sec), DB writes happen asynchronously.',
        complexity: { hit: '<1ms', miss: 'DB query + cache fill', stampede: 'Mutex locking' } },
      { name: 'CDN', visualizer: 'systemdesign', route: '/topics/system-design/CDN/learn', scenarioId: 'cdn',
        explanation: 'Content Delivery Network: cache static assets at edge locations (200+ PoPs worldwide). Geographic routing: user requests nearest edge. Push CDN: upload assets to origin, pull CDN: edge fetches on first request. TTL: cache expiration time. Invalidation: remove cached content before TTL.',
        useCases: ['Static asset delivery', 'Global latency reduction', 'DDoS mitigation'],
        realWorld: 'YouTube: Videos cached at edge PoPs, ~90% of requests served from CDN, <50ms latency globally.',
        complexity: { edge_hit: '<10ms', miss: 'Origin fetch + propagation' } },
      { name: 'Rate Limiter', visualizer: 'systemdesign', route: '/topics/system-design/Rate Limiter/learn', scenarioId: 'rate-limiter',
        explanation: `Controls request rate to prevent abuse. Algorithms: Token Bucket (refillable tokens), Sliding Window Log (timestamp list), Fixed Window Counter (TTL-based). Redis stores counters per user/IP. Response: 429 Too Many Requests with Retry-After header.`,
        useCases: ['API protection', 'DDoS prevention', 'Fair usage enforcement'],
        realWorld: 'Twitter: rate limits API calls per user (15 requests per 15 minutes for standard access).',
        complexity: { check: '~1ms Redis call', storage: 'Per-user counters' } },
      { name: 'Sharding', visualizer: 'systemdesign', route: '/topics/system-design/Sharding/learn', scenarioId: 'sharding',
        explanation: 'Horizontal partitioning: split data across databases. Key-based: hash key % N shards. Range-based: shard by key range (e.g., A-M, N-Z). Directory-based: lookup table maps key to shard. Re-balancing: add/remove shards requires data migration. Consistent hashing: minimizes re-distribution on shard change.',
        useCases: ['Database scaling', 'Write throughput', 'Data distribution'],
        realWorld: 'Instagram: User data sharded by user_id hash across 1000+ database shards.',
        complexity: { lookup: 'O(1) hash', rebalance: 'O(data / shards)' } },
      { name: 'Replication', visualizer: 'systemdesign', route: '/topics/system-design/Replication/learn', scenarioId: 'replication',
        explanation: 'Copy data across servers for fault tolerance. Leader-follower: one leader handles writes, followers replicate. Multi-leader: multiple nodes accept writes (conflict resolution). Quorum: W+R > N for strong consistency. Synchronous: commit after all replicas acknowledge. Asynchronous: leader commits without waiting.',
        useCases: ['High availability', 'Read scaling', 'Disaster recovery'],
        realWorld: 'Uber: Database replication ensures ride data survives AZ failure, read replicas serve analytics queries.',
        complexity: { sync: 'RTT to replicas', async: 'No latency penalty' } },
      { name: 'Message Queue', visualizer: 'systemdesign', route: '/topics/system-design/Message Queue/learn', scenarioId: 'message-queue',
        explanation: 'Async communication: producer sends message, consumer processes later. Decouples services: producer doesn\u2019t wait for consumer. Kafka/RabbitMQ/SQS. Delivery guarantees: at-most-once, at-least-once, exactly-once. Consumer groups: parallel processing. Dead-letter queue: failed messages.',
        useCases: ['Service decoupling', 'Load leveling', 'Async processing'],
        realWorld: 'Uber: Ride events published to Kafka, consumed by notification, analytics, billing services independently.',
        complexity: { produce: 'Milliseconds', consume: 'At consumer rate', rebalance: 'Seconds' } },
      { name: 'Raft Consensus', visualizer: 'systemdesign', route: '/topics/system-design/Raft Consensus/learn', scenarioId: 'raft',
        explanation: `Consensus algorithm: leader elected, followers replicate log. Leader heartbeat: followers detect failure, elect new leader. Log entry replicated to majority before commitment. Strong leader: simplifies reasoning vs Paxos.`,
        useCases: ['Fault-tolerant consensus', 'Distributed databases', 'etcd/Consul'],
        realWorld: 'Kubernetes etcd: stores cluster state using Raft, survives minority node failures.',
        complexity: { election: '50-300ms', replication: 'Committed when quorum acks' } },
    ]
  },
  {
    id: 'microservices', label: 'Microservices', icon: '\uD83D\uDD0C',
    meta: {
      color: 'blue',
      desc: 'Decompose monolith into independent services. Each service owns its data, communicates via APIs. Enables independent scaling, deployment, and technology choices.',
      objectives: [
        'Understand service boundaries and bounded contexts',
        'Learn circuit breaker pattern for resilient inter-service calls',
        'Implement service discovery for dynamic endpoint resolution',
        'Design event-driven communication for loose coupling',
      ],
      keyTopics: ['Bounded Contexts', 'Bulkhead Pattern', 'Service-to-Service Auth', 'Idempotency']
    },
    subtopics: [
      { name: 'Load Balancer', visualizer: 'systemdesign', route: '/visualizer/systemdesign',
        explanation: 'Distributes traffic across service instances. Round-robin, least-connections, IP-hash algorithms. Health checks detect and remove unhealthy instances.',
        useCases: ['Service scaling', 'Traffic distribution', 'Fault tolerance'],
        realWorld: 'Kubernetes Service: round-robin across healthy pod IPs, updated as pods scale.',
        complexity: { routing: 'O(1)', health_check: 'Configurable interval' } },
      { name: 'Cache', visualizer: 'systemdesign', route: '/visualizer/systemdesign',
        explanation: 'Redis/Memcached caching layer reduces latency. Cache-aside, write-through, write-behind patterns. TTL-based expiration, LRU eviction. Cache stampede protection with mutex locks.',
        useCases: ['Latency reduction', 'Database offload', 'Session storage'],
        realWorld: 'Microservice A caches service B responses in Redis, avoiding redundant computation on repeated requests.',
        complexity: { get: '<1ms', set: '<1ms' } },
      { name: 'CDN', visualizer: 'systemdesign', route: '/visualizer/systemdesign',
        explanation: 'Edge caching for static assets. CloudFront, Cloudflare, Akamai. Global PoPs serve content from nearest location. Reduces origin server load by 90%+ for static content.',
        useCases: ['Static content delivery', 'Global latency reduction'],
        realWorld: 'Microservice frontends served via CDN, API calls go directly to backend services.',
        complexity: { edge_hit: '<10ms', propagation: 'Seconds to minutes' } },
      { name: 'Raft', visualizer: 'systemdesign', route: '/visualizer/systemdesign',
        explanation: 'Consensus for distributed coordination. Leader election, log replication, fault tolerance. Used by etcd, Consul for service registry.',
        useCases: ['Service registry consensus', 'Configuration store'],
        realWorld: 'Microservices register with Consul (Raft-based), discover each other via DNS or API.',
        complexity: { election: '50-300ms', quorum: 'Majority required' } },
      { name: 'Circuit Breaker', visualizer: 'microservices', route: '/visualizer/microservices',
        explanation: `Prevents cascading failures: if downstream service fails, open circuit (fail fast without calling), after timeout try again (half-open), if succeed close circuit. Protects system from overload and enables graceful degradation.`,
        useCases: ['Resilience to failures', 'Preventing cascading failures', 'Fast failure signaling'],
        realWorld: 'Uber: If payment service is slow, circuit breaker returns fallback (charge later) instead of blocking rider.',
        complexity: { states: '3 (closed, open, half-open)', timeout: 'Configurable seconds' } },
      { name: 'Service Discovery', visualizer: 'microservices', route: '/visualizer/microservices',
        explanation: `Client needs to find which server handles service. DNS-based: service name resolves to current IP list (updates automatically as services scale). Heartbeat: service registers alive status, removed after N missed heartbeats.`,
        useCases: ['Finding service instances', 'Load balancing across instances', 'Scaling visibility'],
        realWorld: 'WhatsApp: Message service discovers 1000 available message handlers via DNS or consul.',
        complexity: { discovery: 'O(1) DNS', propagation: '100ms latency' } },
      { name: 'API Gateway', visualizer: 'microservices', route: '/visualizer/microservices',
        explanation: `Single entry point for all client requests. Handles: routing to services, authentication, rate limiting, request/response transformation, logging. Aggregates responses from multiple services for client convenience.`,
        useCases: ['Unified interface', 'Cross-cutting concerns', 'Client decoupling'],
        realWorld: 'Instagram: API Gateway routes photo upload to media service, creates post via post service, updates feed.',
        complexity: { latency: 'Single hop', throughput: 'Millions req/sec with proper scaling' } },
      { name: 'Event-Driven', visualizer: 'microservices', route: '/visualizer/microservices',
        explanation: `Services emit events when state changes. Other services subscribe and react asynchronously. Kafka/RabbitMQ handle event transport. Enables loose coupling, but adds complexity: ordering, idempotency, eventual consistency.`,
        useCases: ['Decoupling services', 'Async processing', 'Complex workflows'],
        realWorld: 'YouTube: Video upload emits event, triggering transcoding, thumbnail generation, recommendation update independently.',
        complexity: { latency: '100ms-seconds', consistency: 'Eventual' } },
    ]
  },
  {
    id: 'database', label: 'Database', icon: '\uD83D\uDDFC\uFE0F',
    meta: {
      color: 'yellow',
      desc: 'Database internals: B-Tree indexing, query planning, ACID transactions, join algorithms, and query optimization.',
      objectives: [
        'Understand B-Tree structure and why databases use it',
        'Read query plans: identify full table scans vs index scans',
        'Trace transaction isolation: dirty reads, phantom reads, serialization',
        'Compare join algorithms: nested loop, hash join, sort-merge',
      ],
      keyTopics: ['B-Tree Balance', 'Cardinality Estimation', 'MVCC', 'Join Ordering']
    },
    subtopics: [
      { name: 'B-Tree', visualizer: 'database', route: '/visualizer/database',
        explanation: `Multi-way tree for disk-based indexing. Each node holds multiple keys, branching to children. Balanced: all leaves at same depth O(log n). Range queries scan leaves efficiently. Most database indexes use B-Tree variants (B+ Tree).`,
        useCases: ['Database indexes', 'File system allocation', 'Range queries'],
        realWorld: 'PostgreSQL, MySQL use B-Tree for indexes on disk.',
        complexity: { search: 'O(log n)', range_scan: 'O(k) where k=results', insert: 'O(log n)' } },
      { name: 'Query Planning', visualizer: 'database', route: '/visualizer/database',
        explanation: `Optimizer chooses execution plan: full table scan vs index scan, join order, join algorithm. Cost estimation: cardinality (rows matching), I/O cost, CPU cost. Bad plan causes query to run hours instead of milliseconds.`,
        useCases: ['Query optimization', 'Index selection', 'Performance tuning'],
        realWorld: 'EXPLAIN shows plan; ANALYZE gathers statistics for cost estimation.',
        complexity: { planning: 'Seconds for complex queries', execution: 'Varies wildly' } },
      { name: 'Transactions', visualizer: 'database', route: '/visualizer/database',
        explanation: `ACID transactions guarantee consistency even with failures. Isolation levels: Read Uncommitted (dirty reads), Read Committed (locks until commit), Repeatable Read (snapshot isolation), Serializable (true serializability). Locking & MVCC implement isolation.`,
        useCases: ['Money transfers', 'Critical updates', 'Consistency guarantees'],
        realWorld: 'Bank transfer: debit account + credit account in single transaction, all-or-nothing.',
        complexity: { locking: 'Microseconds-milliseconds', mvcc: 'No blocking reads' } },
      { name: 'Joins', visualizer: 'database', route: '/visualizer/database',
        explanation: `Combine rows from multiple tables: nested loop (slow, O(n*m)), hash join (O(n+m), good for large tables), sort-merge join (O(n log n + m log m), good for pre-sorted). Index join if index available.`,
        useCases: ['Combining related data', 'Complex queries'],
        realWorld: 'SELECT users.name FROM users JOIN orders WHERE orders.user_id = users.id',
        complexity: { nested_loop: 'O(n*m)', hash: 'O(n+m)', sort_merge: 'O(n log n)' } },
    ]
  },
  {
    id: 'redis', label: 'Redis', icon: '\uD83D\uDD34',
    meta: {
      color: 'red',
      desc: 'In-memory data store for caching and real-time data. Blazingly fast (microseconds), but limited by RAM. Data types: strings, lists, sets, hashes, sorted sets.',
      objectives: [
        'Understand Redis data structures and their use cases',
        'Learn Pub/Sub messaging pattern',
        'Understand Redis Cluster sharding (16384 hash slots)',
        'Master pipelining for high-throughput operations',
      ],
      keyTopics: ['Single-Threaded Event Loop', 'Hash Slot Sharding', 'RDB/AOF Persistence', 'Sentinel HA']
    },
    subtopics: [
      { name: 'Data Types', visualizer: 'redis', route: '/visualizer/redis',
        explanation: `String: key\u2192value, O(1) get/set. List: linked list, O(1) push/pop head/tail. Set: unique elements, O(1) add/contains. Hash: object-like, O(1) field access. Sorted Set: ordered by score, O(log n) add/remove, O(1) score access.`,
        useCases: ['Session cache', 'Rate limiting counters', 'Leaderboards (sorted set)', 'User profiles (hash)'],
        realWorld: 'Uber: Driver locations cached in Redis as geo hash with expiration.',
        complexity: { string: 'O(1)', list: 'O(1) ops', set: 'O(1) avg', zset: 'O(log n)' } },
      { name: 'Pub/Sub', visualizer: 'redis', route: '/visualizer/redis',
        explanation: `Publish-Subscribe messaging: publishers send to channels, subscribers receive. Channels identified by string name. Loose coupling: pub doesn't know subs. Fire-and-forget: message lost if no subscribers.`,
        useCases: ['Real-time notifications', 'Event distribution', 'Broadcasting'],
        realWorld: 'Instagram: User sends message, published to room channel, all connected users subscribe & receive in <100ms.',
        complexity: { publish: 'O(subscribers)', subscribe: 'O(1) per sub' } },
      { name: 'Cluster', visualizer: 'redis', route: '/visualizer/redis',
        explanation: `Redis Cluster: hash-slot based sharding. 16384 slots, each node owns subset. Hash slot = CRC16(key) % 16384. Node failure: remaining nodes own slots if quorum available. Slave replication within cluster.`,
        useCases: ['Scaling beyond single server RAM', 'High availability', 'Distributed caching'],
        realWorld: 'YouTube: Redis cluster stores 100B+ key-values across 1000+ nodes.',
        complexity: { redirect: 'Client redirected to correct node', failover: 'Seconds' } },
      { name: 'Pipelining', visualizer: 'redis', route: '/visualizer/redis',
        explanation: `Send multiple commands in batch without waiting for responses. Reduces round trips: 1000 commands in 1 network call instead of 1000 calls. Server executes sequentially, returns all results. Huge throughput boost.`,
        useCases: ['Bulk operations', 'High throughput', 'Background jobs'],
        realWorld: 'Analytics: increment 100K counters in single pipeline call.',
        complexity: { throughput: '100x improvement', latency: 'Same per-command' } },
    ]
  },
  {
    id: 'docker', label: 'Docker', icon: '\uD83D\uDC33',
    meta: {
      color: 'blue',
      desc: 'Container platform: package app + dependencies in image, run anywhere. Lightweight VM: own filesystem, network, process space. Enables reproducible deployments.',
      objectives: [
        'Understand image layers and Dockerfile optimization',
        'Trace container lifecycle: create \u2192 start \u2192 stop \u2192 remove',
        'Learn Docker networking: bridge, host, overlay modes',
        'Master docker-compose for multi-container applications',
      ],
      keyTopics: ['Union Filesystem', 'PID 1 Signal Handling', 'Port Mapping', 'Dependency Ordering']
    },
    subtopics: [
      { name: 'Image Layers', visualizer: 'docker', route: '/visualizer/docker',
        explanation: `Dockerfile commands create layers: FROM (base), RUN (execute), COPY (add files), EXPOSE (port). Each layer is cached: changing RUN command only rebuilds from that line onward. Union filesystem combines layers: READ-ONLY + read-write container layer.`,
        useCases: ['Reproducible builds', 'Efficient caching', 'Minimal images'],
        realWorld: 'Uber: Multi-stage build: compile in builder image, copy binary to tiny runtime image (50MB vs 500MB).',
        complexity: { build: 'Seconds with cache', layer: 'Each is separate file' } },
      { name: 'Container Lifecycle', visualizer: 'docker', route: '/visualizer/docker',
        explanation: `Docker events: create \u2192 start \u2192 running \u2192 pause/unpause \u2192 stop \u2192 remove. Init process (PID 1) handles signals. ENTRYPOINT: command to run, CMD: default args. Graceful shutdown: app should handle SIGTERM.`,
        useCases: ['App orchestration', 'Signal handling', 'Cleanup on exit'],
        realWorld: 'Web server: ENTRYPOINT nginx, on SIGTERM flush logs & exit.',
        complexity: { startup: '10-100ms', signal_to_exit: '<1s' } },
      { name: 'Networking', visualizer: 'docker', route: '/visualizer/docker',
        explanation: `Bridge network (default): each container gets IP on bridge subnet, containers talk by IP. Port mapping: map container port to host port (e.g., 8080\u219280). Host network: container shares host IP & ports.`,
        useCases: ['Inter-container communication', 'Port mapping', 'Multi-container apps'],
        realWorld: 'Docker-compose: web service on port 8000, nginx reverse proxy maps 80 to container 8000.',
        complexity: { DNS: 'Automatic service discovery by name', latency: '<1ms' } },
      { name: 'Compose', visualizer: 'docker', route: '/visualizer/docker',
        explanation: `docker-compose.yml defines multi-container app: services (containers), networks, volumes (persistent storage). Single command: docker-compose up starts all. Dependency ordering: web depends_on db, waits for db first.`,
        useCases: ['Local development', 'Integration testing', 'Simple deployments'],
        realWorld: 'Dev environment: database + web + cache running together, matches production.',
        complexity: { startup: '5-20 seconds', management: 'Single YAML file' } },
    ]
  },
  {
    id: 'networking', label: 'Networking', icon: '\uD83C\uDF10',
    meta: {
      color: 'purple',
      desc: 'Networks connect computers: TCP reliable ordered delivery, UDP fast unreliable. HTTP protocols enable web. DNS resolves names to IPs. Load balancers distribute traffic.',
      objectives: [
        'Trace TCP 3-way handshake: SYN, SYN-ACK, ACK',
        'Understand HTTP/1.1 vs HTTP/2 multiplexing vs HTTP/3 QUIC',
        'Learn DNS resolution: recursive, TTL caching, anycast routing',
        'Compare load balancing algorithms: round-robin, least-connections, IP-hash',
      ],
      keyTopics: ['OSI Model', 'TCP Congestion Control', 'TLS Handshake', 'Anycast Routing']
    },
    subtopics: [
      { name: 'TCP Handshake', visualizer: 'networking', route: '/visualizer/networking',
        explanation: `3-way handshake: SYN (client sends, sequence=x), SYN-ACK (server responds, sequence=y, ack=x+1), ACK (client confirms, ack=y+1). Establishes sequence numbers for reliable delivery. Connection states: LISTEN \u2192 SYN_RCVD \u2192 ESTABLISHED \u2192 FIN_WAIT \u2192 TIME_WAIT.`,
        useCases: ['Connection establishment', 'Reliable communication', 'Flow control setup'],
        realWorld: 'Browser connecting to server: 3 packets before HTTP request can be sent.',
        complexity: { latency: 'RTT \u00D7 1.5', overhead: '3 packets' } },
      { name: 'HTTP', visualizer: 'networking', route: '/visualizer/networking',
        explanation: `HTTP/1.1: persistent connections, request-response model, one request per connection at a time. HTTP/2: multiplexing (multiple streams in 1 connection), server push, binary framing. HTTP/3: QUIC protocol, faster connection setup.`,
        useCases: ['Web requests', 'APIs', 'Browser-server communication'],
        realWorld: 'Modern browsers use HTTP/2 multiplexing: request 100 resources in parallel on 1 connection.',
        complexity: { latency: '50-500ms depending on network', throughput: 'Limited by TCP window' } },
      { name: 'DNS', visualizer: 'networking', route: '/visualizer/networking',
        explanation: `Domain Name System: resolves "youtube.com" to IP. Recursive query: client asks resolver, resolver queries root\u2192TLD\u2192authoritative. Caching: local cache, ISP cache, CloudFlare (8.8.8.8) public DNS. TTL controls expiration.`,
        useCases: ['Name resolution', 'Load balancing (round-robin DNS)', 'CDN routing'],
        realWorld: 'YouTube.com resolves to different IPs by geography: US\u2192US CDN, India\u2192India CDN.',
        complexity: { latency: '1-100ms (cached fast)', ttl: '5min-24hrs' } },
      { name: 'Load Balancer', visualizer: 'networking', route: '/visualizer/networking',
        explanation: `Distributes traffic across servers: round-robin (rotate), least-connections (fewest current), weighted, IP-hash (same user\u2192same server). Health checks: periodically test servers, remove unhealthy ones. Sticky sessions: session affinity.`,
        useCases: ['Scaling web servers', 'High availability', 'Request distribution'],
        realWorld: 'Uber: Load balancer distributes API requests across 1000 backend servers, removes unresponsive ones.',
        complexity: { latency: '<1ms', failover: 'Seconds after health check failure' } },
    ]
  },
  {
    id: 'os', label: 'Operating Systems', icon: '\uD83D\uDDA5\uFE0F',
    meta: {
      color: 'gray',
      desc: 'Operating System manages hardware: CPU scheduling, memory management, I/O. Efficiency critical: scheduling algorithm impacts responsiveness, memory allocation impacts performance.',
      objectives: [
        'Compare CPU scheduling: FCFS, Round-Robin, Priority, CFS',
        'Understand paging: demand paging, page replacement algorithms',
        'Trace virtual memory: MMU translation, page tables, TLB caching',
      ],
      keyTopics: ['Context Switch', 'TLB Miss', 'Swap', 'Fragmentation']
    },
    subtopics: [
      { name: 'Scheduler', visualizer: 'os', route: '/visualizer/os',
        explanation: `CPU Scheduler decides which process runs next. FCFS (First-Come-First-Served): simple but poor responsiveness. Round-Robin: each process gets time slice (quantum), fairness but many context switches. Priority scheduling: high-priority tasks first.`,
        useCases: ['Fair CPU allocation', 'Responsiveness', 'Real-time guarantees'],
        realWorld: 'Linux CFS (Completely Fair Scheduler): aims equal CPU time for all processes.',
        complexity: { switch: '1-10 microseconds', fairness: 'Quantum-dependent' } },
      { name: 'Paging', visualizer: 'os', route: '/visualizer/os',
        explanation: `Divide memory into pages (4KB), disk into swap. Demand paging: load page only when accessed. Page replacement: LRU (least recently used), FIFO, Second-Chance. Page fault: process blocks while page loaded (expensive ~10ms).`,
        useCases: ['Virtual memory', 'Memory overcommit', 'Protection (page boundaries)'],
        realWorld: 'Process thinks it has 1TB RAM, actually only 4GB available.',
        complexity: { fault: '10-100ms per fault', tlb_hit: 'Hardware cached, <1ns' } },
      { name: 'Virtual Memory', visualizer: 'os', route: '/visualizer/os',
        explanation: `Each process has own address space (logical addresses). MMU (Memory Management Unit) translates to physical. Page table: logical page # \u2192 physical frame #. TLB: hardware cache of recent translations for speed.`,
        useCases: ['Process isolation', 'Memory protection', 'Overcommit', 'Relocation'],
        realWorld: 'Two processes with same virtual address 0x1000 map to different physical memory.',
        complexity: { translation: 'TLB <1ns, memory 100ns', overhead: 'Page table memory' } },
    ]
  },
  {
    id: 'ai', label: 'AI / Transformers', icon: '\uD83E\uDD16',
    meta: {
      color: 'green',
      desc: 'Artificial Intelligence: Transformers revolutionized NLP. Attention mechanism allows words to attend to all other words simultaneously. Enables understanding context, relationships.',
      objectives: [
        'Understand Transformer architecture: encoder-decoder with attention',
        'Trace attention computation: Q, K, V matrices and softmax scoring',
        'Learn embedding spaces: semantic relationships via vector arithmetic',
      ],
      keyTopics: ['Self-Attention', 'Multi-Head Attention', 'Positional Encoding', 'Masking']
    },
    subtopics: [
      { name: 'Transformer', visualizer: 'ai', route: '/visualizer/ai',
        explanation: `Neural network architecture with encoder (processes input), decoder (generates output). Attention layers: each position "attends to" all other positions. Feed-forward layers: non-linearity. Positional encoding: encodes word order. Layer normalization: stabilizes training.`,
        useCases: ['Language modeling', 'Machine translation', 'Q&A systems'],
        realWorld: 'GPT-4: 1.7 trillion parameters, trained on text corpus, predicts next word.',
        complexity: { params: 'Billions-trillions', inference: '50-100ms per token' } },
      { name: 'Attention', visualizer: 'ai', route: '/visualizer/ai',
        explanation: `Scaled dot-product attention: Query (Q), Key (K), Value (V). Score = softmax(Q \u00B7 K^T / \u221Ad_k) \u00B7 V. Multi-head: multiple attention heads in parallel for different representations. Self-attention: Q=K=V (same input), attends to itself.`,
        useCases: ['Capturing relationships', 'Long-range dependencies', 'Interpretability'],
        realWorld: 'Attention weights show which words influenced decision: "bank" attends to "river" not "money".',
        complexity: { time: 'O(n\u00B2) where n=sequence_length', memory: 'O(n\u00B2)' } },
      { name: 'Embeddings', visualizer: 'ai', route: '/visualizer/ai',
        explanation: `Convert words/tokens to vectors: Word2Vec (skip-gram, CBOW), GloVe (global vectors), contextual (BERT/GPT embed word differently based on context). Semantic similarity: cosine distance between vectors. Vector arithmetic: king-man+woman \u2248 queen.`,
        useCases: ['Semantic search', 'Similarity', 'Downstream tasks'],
        realWorld: 'YouTube: Video embeddings cluster similar videos, enables recommendations.',
        complexity: { dimension: '300-1536 typical', training: 'Hours on corpus' } },
    ]
  },
  {
    id: 'distributed', label: 'Distributed Systems', icon: '\uD83C\uDF0D',
    meta: {
      color: 'purple',
      desc: 'Distributed Systems coordinate multiple machines: Raft for consensus, CAP theorem describes trade-offs. Handling failures, network partitions, eventual consistency is complex.',
      objectives: [
        'Understand Raft consensus: leader election, log replication, safety',
        'Apply CAP theorem: system design trade-offs between consistency and availability',
        'Learn 2PC distributed transaction protocol and its blocking problem',
      ],
      keyTopics: ['Quorum', 'Clock Skew', 'Fencing Tokens', 'Saga Pattern']
    },
    subtopics: [
      { name: 'Raft', visualizer: 'distributed', route: '/visualizer/distributed',
        explanation: `Consensus algorithm: leader elected, followers replicate log. Leader heartbeat: followers detect failure, elect new leader. Log entry replicated to majority before commitment. Strong leader: simplifies reasoning vs Paxos.`,
        useCases: ['Fault-tolerant consensus', 'Distributed databases', 'etcd/Consul'],
        realWorld: 'Kubernetes etcd: stores cluster state using Raft, survives minority node failures.',
        complexity: { election: '50-300ms', replication: 'Committed when quorum acks' } },
      { name: 'CAP Theorem', visualizer: 'distributed', route: '/visualizer/distributed',
        explanation: `Consistency-Availability-Partition tolerance: can't have all 3. CP: sacrifice availability (lock until quorum responds). AP: sacrifice consistency (eventual consistency). Partitioned: CA impossible. Real systems: AP with quorum reads/writes.`,
        useCases: ['System design trade-offs', 'Consistency vs availability'],
        realWorld: 'Instagram: eventual consistency (eventual=hours) for feed (AP), strong consistency for payment (CP).',
        complexity: { tradeoff: 'Fundamental impossibility', design: 'Unavoidable choice' } },
      { name: '2PC', visualizer: 'distributed', route: '/visualizer/distributed',
        explanation: `Two-Phase Commit: coordinator asks participants to prepare (acquire locks, validate), then commit (write) or abort (rollback). Blocking: locks held during prepare phase. Slow for WAN (geographic). Rarely used; saga pattern preferred.`,
        useCases: ['Distributed transactions', 'ACID across systems'],
        realWorld: 'Bank transfer: debit service & credit service participate in 2PC to ensure both succeed or both fail.',
        complexity: { blocking: 'Locks held', latency: 'Seconds for WAN', failures: 'Complex recovery' } },
    ]
  },
  {
    id: 'concurrency', label: 'Concurrency', icon: '\uD83E\uDDF5',
    meta: {
      color: 'purple',
      desc: 'Multiple threads accessing shared resources simultaneously. Race conditions, deadlocks, data corruption possible. Synchronization primitives protect critical sections.',
      objectives: [
        'Understand mutex: mutual exclusion for critical sections',
        'Trace semaphore counting: coordinate producer-consumer',
        'Visualize deadlock: circular wait condition and prevention',
      ],
      keyTopics: ['Atomicity', 'Race Condition', 'Deadlock Prevention', 'Condition Variables']
    },
    subtopics: [
      { name: 'Mutex', visualizer: 'concurrency', route: '/visualizer/concurrency',
        explanation: `Mutual exclusion lock: only one thread in critical section at a time. Lock owner must release (or code bugs). Contention: if many threads wait, queue forms. Spin lock: thread loops checking lock (CPU waste, useful for microsecond locks).`,
        useCases: ['Protecting shared state', 'Counter increments', 'Complex data structure updates'],
        realWorld: 'Uber: Mutex protects active ride count when multiple request handlers decrement simultaneously.',
        complexity: { acquire: 'O(1)', contention: '0-100\u00B5s' } },
      { name: 'Semaphore', visualizer: 'concurrency', route: '/visualizer/concurrency',
        explanation: `Counter-based synchronization. Binary semaphore (0/1): like mutex. Counting semaphore (n): up to n threads enter. Producer-consumer: semaphores track empty/full buffer slots, coordinate producers/consumers.`,
        useCases: ['Producer-consumer', 'Resource pooling', 'Limiting concurrency'],
        realWorld: 'WhatsApp: Semaphore limits message processing threads to 100 (connection pool size).',
        complexity: { wait: 'Block on counter=0', signal: 'Wake one waiter' } },
      { name: 'Producer-Consumer', visualizer: 'concurrency', route: '/visualizer/concurrency',
        explanation: `Producer creates items, puts in bounded buffer. Consumer takes items from buffer. If buffer full: producer waits. If buffer empty: consumer waits. Semaphores coordinate: empty=n (all slots empty), full=0 (no full slots).`,
        useCases: ['Decoupling producers/consumers', 'Work queues', 'Rate limiting'],
        realWorld: 'YouTube: Upload queue has max 1000 videos. Transcoders (consumers) take from queue, uploaders wait if full.',
        complexity: { throughput: 'Limited by buffer size \u00D7 producer/consumer speed' } },
      { name: 'Deadlock', visualizer: 'concurrency', route: '/visualizer/concurrency',
        explanation: 'Four conditions: mutual exclusion, hold-and-wait, no preemption, circular wait. Prevention: lock ordering (acquire locks in fixed order), tryLock with timeout, lock hierarchy. Detection: wait-for graph cycle detection. Recovery: kill one thread.',
        useCases: ['Resource allocation', 'Database locking', 'Multi-threaded systems'],
        realWorld: 'Java: synchronized blocks must be ordered to avoid deadlock. "Lock A then B" protocol.',
        complexity: { detection: 'O(n\u00B2) wait-for graph', prevention: 'Lock ordering' } },
    ]
  },
];

const CASESTUDIES = {
  'system-design': {
    overview: `Design scalable systems supporting millions of users. Uber ride-hailing demonstrates: distributed payments, real-time matching, geospatial queries, message queuing.`,
    casestudies: {
      Uber: {
        components: [
          { name: 'API Gateway', desc: 'Route requests to services, rate limiting, auth validation' },
          { name: 'Rider Service', desc: 'User accounts, ride history, preferences' },
          { name: 'Driver Service', desc: 'Driver profiles, vehicle info, ratings' },
          { name: 'Matching Engine', desc: 'Algorithm to match riders with nearby drivers' },
          { name: 'Redis Cache', desc: 'Cache driver locations, active rides, hot data' },
          { name: 'Pricing Service', desc: 'Calculate fare based on distance, surge pricing' },
          { name: 'Payment Service', desc: 'Process payments, charge user cards' },
          { name: 'Trip Service', desc: 'Store trip details, route, timestamps' },
          { name: 'Notification Service', desc: 'Send driver/rider notifications via push/email' },
          { name: 'Kafka', desc: 'Event streaming for async processing' },
          { name: 'PostgreSQL', desc: 'Durable storage for user/trip data' },
          { name: 'Geospatial DB', desc: 'Query drivers near location (radius search)' },
        ],
        flow: [
          '1. Rider opens app, requests ride',
          '2. Request hits API Gateway, routed to Rider Service',
          '3. Matching Engine queries Redis for nearby drivers, runs matching algorithm',
          '4. Pricing Service calculates fare estimate',
          '5. Show rider list of available drivers',
          '6. Rider selects driver, Payment Service pre-authorizes card',
          '7. Event published to Kafka: RideAccepted',
          '8. Notification Service sends both driver & rider push notifications',
          '9. Trip Service stores trip record in PostgreSQL',
          '10. During trip: driver app sends location updates, driver-location-service updates Redis every 5 seconds',
          '11. After drop-off: final fare calculated, payment charged',
          '12. Trip Service updates trip status: completed'
        ],
        challenges: [
          'Real-time matching at scale (1M+ concurrent users)',
          'Geospatial queries for "drivers near me"',
          'Exactly-once payment processing',
          'Handling driver/rider disconnects gracefully',
          'Surge pricing algorithm fairness',
          'Database consistency across payment systems'
        ]
      },
      WhatsApp: {
        overview: 'Real-time messaging for 2B+ users: 100B messages/day, persistent WebSocket connections, E2EE, <100ms delivery latency. Distributed at global scale across 500+ chat servers.',
        architectureTiers: [
          { name: 'Client Layer', desc: 'Mobile apps (Android/iOS/Web) with persistent WSS connections. Protobuf binary framing. Local SQLite cache for offline access. Background service for push wake.' },
          { name: 'Edge Layer', desc: 'Anycast DNS (Route53) routes to nearest PoP. CloudFront/Global LB terminates TLS. Proxy Protocol preserves client IP to backend.' },
          { name: 'Gateway Layer', desc: 'WebSocket Gateway manages ~500K persistent connections per node. Connection registry in Redis. Heartbeat every 30s. Credit-based backpressure.' },
          { name: 'Service Layer', desc: 'Chat Service (message routing, dedup, rate limiting, content scan). Presence Service (Redis Pub/Sub). Media Service (upload, transcode, CDN). Group Service (fanout logic). User Service (profiles, contacts).' },
          { name: 'Message Bus', desc: 'Kafka cluster (20+ nodes). Topics: msg_sent, media_processing, group_fanout, delivery_acks, spam_scan. 48h retention. Partitioned by recipient hash.' },
          { name: 'Data Layer', desc: 'Cassandra (80+ node multi-DC cluster for messages). PostgreSQL (1000+ shards for users, contacts, groups). Redis Cluster (presence, sessions, rate limits, dedup). S3/Ceph (media blobs).' },
        ],
        components: [
          { name: 'Connection Manager', desc: 'Per-node registry of 500K+ WebSocket connections. Write deadline (10s) prevents slow-client memory bloat. Goroutine-per-connection read loop. Graceful drain on shutdown.' },
          { name: 'Chat Service', desc: 'Core message router. Dedup via Redis SETNX (24h TTL). Rate limit: 30 msg/s per sender. Content scan via Kafka async. Batch write to Cassandra (sender outbox + receiver inbox + conversation view).' },
          { name: 'Presence Service', desc: 'Redis key-value with TTL: presence:{phone} expires in 120s. Heartbeat every 30s resets TTL. Pub/Sub notifies contacts. Background job marks expired as offline. Privacy setting: hide last seen.' },
          { name: 'Group Service', desc: 'Fanout-on-write for groups <1024 members: write msg to each member inbox. Fanout-on-read for communities: single group timeline, cursor-based read. Cassandra LWT for membership changes.' },
          { name: 'Media Service', desc: 'Signed-URL upload to S3. Transcoding queue (Kafka) → workers resize images (4 sizes), transcode video (H.264 360p-1080p), compress audio (OPUS). Thumbnails E2EE. CDN delivery with 5min signed URLs.' },
          { name: 'Delivery Service', desc: 'Kafka consumer group reads msg_sent. Checks Redis presence. If online: push via WebSocket. If offline: enqueue push notification (FCM/APNS). Update delivery status in Cassandra.' },
          { name: 'E2EE Service', desc: 'Key directory (server is dumb): stores encrypted prekey bundles. X3DH key agreement + Double Ratchet. Perfect Forward Secrecy. Deniable authentication. Server never sees plaintext keys.' },
          { name: 'Auth Service', desc: 'SMS OTP verification. JWT session tokens (30 day expiry). Device registration (max 4 devices per account). Multi-device: same phone, independent E2EE sessions.' },
          { name: 'Push Notification Service', desc: 'Coalesces pending messages into single FCM/APNS payload. Rate-limited to avoid FCM throttling. Batch: 10 messages → 1 push. Payload: {msg_count, sender, preview}.' },
          { name: 'Rate Limiter', desc: 'Token bucket per sender (30 msg/s). Sliding window per conversation (100 msg/min). IP-level for unauthenticated. Redis-backed with Lua atomic check+decrement.' },
        ],
        flow: [
          '1. User opens app → app establishes WSS connection: DNS → TCP :443 → TLS 1.3 → HTTP Upgrade → 101 Switching Protocols',
          '2. Auth: client sends JWT session token as first binary frame. Server validates, registers connection in Redis, marks presence online',
          '3. User types message → client generates UUID v7 msg_id, encrypts with Signal Protocol session key, sends Protobuf frame',
          '4. Chat Service receives: checks dedup (Redis SETNX by msg_id), validates receiver exists, rate limit check, content scan (async via Kafka)',
          '5. Batch write to Cassandra: messages_by_user (sender inbox, direction=sent), messages_by_user (receiver inbox, direction=received), messages_by_conversation',
          '6. Produce to Kafka topic msg_sent (partition: hash(recipient) → ordered delivery per user)',
          '7. Ack to sender: "message persisted" → client shows single ✓',
          '8. Delivery Service consumer reads msg_sent topic. Checks Redis presence:{recipient}',
          '9. IF ONLINE: push Protobuf frame via recipient WebSocket. Recipient acks msg_id → "delivered" status → sender sees ✓✓',
          '10. IF OFFLINE: enqueue push notification to FCM/APNS. Push payload wakes app, app reconnects, fetches missed messages via cursor-based sync',
          '11. Recipient reads message → client sends read receipt → server updates status=read → sender sees blue ✓✓',
          '12. Group message (512 members): ChatService writes to group_messages Cassandra table. Kafka group_fanout topic → Fanout Workers write to each member inbox (fanout-on-write). Each member reads inbox (O(1))',
          '13. Media message: client uploads encrypted blob via REST → Media Service stores in S3 → Kafka transcode job → workers generate thumbnails → CDN invalidation → trigger message send with media_id + encryption_key hash',
          '14. Heartbeat: server pings every 30s, client must respond within 10s. Missed heartbeat → connection considered dead → presence offline after 120s TTL expiry',
        ],
        challenges: [
          '100B+ messages/day across 2B+ users — need write-optimized DB (Cassandra)',
          'Sub-100ms message delivery globally — anycast routing + edge PoPs + persistent WSS',
          'Offline message queueing for days/weeks — Kafka 48h retention + push notifications',
          'Group messaging consistency for 1024 members — fanout-on-write + LWT for membership',
          'End-to-end encryption overhead — Signal Protocol ratchet per message, key exchange on first contact',
          'Media deduplication and compression — content-addressable storage, CDN caching',
          'Presence at global scale — Redis Pub/Sub with TTL, not persistence',
          'Cross-region data locality — GDPR, each region has local Cassandra + Kafka + Redis',
        ],
        databaseSchema: {
          rationale: 'WhatsApp uses Cassandra for messages (write-optimized, linear scale, multi-DC) and PostgreSQL for user data (transactions, consistency). Redis for ephemeral state.',
          tables: [
            { name: 'messages_by_user', engine: 'Cassandra', desc: 'Per-user inbox/outbox. Partition: (user_id, bucket). Clustering: created_at DESC, msg_id. Bucket = yyyy-MM-dd-HH prevents unbounded partition growth. Write-optimized wide rows.' },
            { name: 'messages_by_conversation', engine: 'Cassandra', desc: 'Chat history view. Partition: (conversation_id, bucket). conversation_id = sorted(user1, user2) for 1:1. Clustering: created_at DESC. LIMIT 50 for chat open.' },
            { name: 'group_messages', engine: 'Cassandra', desc: 'Group timeline. Partition: (group_id, bucket). Used for fanout-on-read in communities. Cursor-based pagination with last_seen_msg_id per member.' },
            { name: 'users', engine: 'PostgreSQL', desc: 'Phone, username, about, avatar_hash, settings (JSONB privacy prefs), E2EE prekey bundle (identity_key, signed_prekey, one_time_prekeys[]). Sharded by hash(phone) % 1000.' },
            { name: 'contacts', engine: 'PostgreSQL', desc: 'Denormalized: (user_phone, contact_phone, display_name, muted_until, added_at). Per-user contact list for fast lookup. PRIMARY KEY (user_phone, contact_phone).' },
            { name: 'groups', engine: 'PostgreSQL', desc: 'Group metadata: name, description, avatar_hash, settings (JSONB), revision (monotonic counter for sync). group_members: (group_id, user_phone, role, added_by). LWT for concurrency.' },
            { name: 'presence:{phone}', engine: 'Redis', desc: 'Ephemeral key-value with TTL. Value: {server, status, ts}. TTL: 120s, refreshed by heartbeat every 30s. No persistence — transient by design.' },
          ],
          sharding: 'Cassandra: virtual nodes (256/node), consistent hashing token ring. PostgreSQL: application-level shard = hash(phone) % N (1000 shards). Redis Cluster: 16384 hash slots, CRC16(key) % 16384.',
          consistency: 'Messages: Write ONE (any replica), Read QUORUM. Presence: Eventual (ok if stale). Group membership: LWT (linearizable). User profile: Write QUORUM, Read QUORUM.',
        },
        protocolFlow: {
          wsLifecycle: 'DNS lookup → TCP :443 connect → TLS 1.3 handshake → HTTP GET /chat Upgrade: websocket → 101 Switching Protocols → Auth frame (JWT) → Bidirectional binary Protobuf frames → Ping/Pong every 30s → Close frame → TCP FIN',
          e2ee: 'Signal Protocol: X3DH key agreement on first contact (DH with Identity Key, Signed Prekey, Ephemeral Key, One-time Prekey → Shared Secret via KDF). Double Ratchet per message (Root Key → Chain Keys → Message Keys, ratchet step per message). Perfect Forward Secrecy: compromise of long-term key ≠ past messages.',
          dedup: 'Client generates UUID v7 per message. Server Redis SETNX with 24h TTL. If key exists → duplicate, drop silently. Fallback: Cassandra read by msg_id if Redis unavailable.',
          backpressure: 'Credit-based: server grants N credits on connect. Client decrements per message. Server sends Grant frame when buffer < 50%. Client stops sending at 0 credits. Prevents slow-client OOM on 500K connections.',
        },
        failureModes: [
          { name: 'Cassandra Node Failure', desc: 'RF=3, node dies → hinted handoff to remaining replicas (3h window). If dead >3h: nodetool repair needed. Impact: Write ONE still succeeds on other replicas. Read QUORUM works with N1+N2. User sees: slight delay.' },
          { name: 'Kafka Rebalance Death Spiral', desc: 'Consumer crashes → rebalance → all consumers STOP (Eager protocol) → lag spikes → some consumers overloaded → more crashes. Mitigation: Cooperative rebalancing (subset revoke), static group membership, partition count = C × 2.' },
          { name: 'Cache Stampede', desc: 'Key expires → 10K concurrent requests miss cache → all hit Cassandra → DB melts → circuit breakers open → cascading failure. Mitigation: stale-while-revalidate, per-key mutex, jittered TTL (+random(0, TTL/10)).' },
          { name: 'Retry Storm', desc: 'Cassandra hiccup (500ms) → client timeout (200ms) → retry with 100ms timeout → 10x requests → DB slows more → more retries. Mitigation: exponential backoff (50ms × 2^attempt) + random jitter ±50%, cap at 10s.' },
          { name: 'Push Notification Flood', desc: 'Server reboot → 100M reconnect → each fetches offline → push burst to FCM → FCM rate-limits → notifications dropped. Mitigation: exponential reconnect backoff, batching (10 messages = 1 push).' },
          { name: 'Hot Partition', desc: 'Very active user (100K msgs/hour) → Cassandra partition grows large. Mitigation: time bucketing (yyyy-MM-dd-HH) limits partition per user per hour. Further sub-bucket by userId:HH-hour if needed.' },
        ],
        performance: {
          p50: '35ms',
          p95: '120ms',
          p99: '450ms',
          breakdown: [
            'Client processing: 5ms (encrypt, encode Protobuf, write to TCP buffer)',
            'Network RTT: 10ms (anycast to nearest PoP, TLS handshake cached)',
            'Server processing: 5ms (decode, dedup, rate limit, validate, route)',
            'Cassandra write: 10ms (commit log + memtable, Write ONE consistency)',
            'Kafka produce: 5ms (ack=1, in-memory write)',
            'WebSocket delivery: 10ms (network to receiver, frame decode, decrypt)',
          ],
          bottlenecks: [
            'WebSocket Gateway: file descriptors (ulimit 1M+), write buffer bloat under backpressure',
            'Chat Service: goroutine scheduling at 500K+ goroutines (Go M:N scheduler, GOMAXPROCS tuning)',
            'Cassandra write: commit log disk I/O (NVMe, separate disk for commitlog)',
            'Kafka: disk throughput for retention (JBOD, multiple disks, rack awareness)',
            'Redis: single-threaded command processing (Cluster mode, avoid KEYS/SMEMBERS)',
          ],
        },
        infraEvolution: [
          { phase: '1. Monolith (0-1M)', desc: 'Single Python/Django server + PostgreSQL + RabbitMQ. All-in-one: chat, auth, media. No sharding. Manual backups.' },
          { phase: '2. Vertical Split (1M-100M)', desc: 'Chat server (Erlang) handles connections. Separate Auth (Python) + Media (Python) servers. PostgreSQL for users, S3-compat for media.' },
          { phase: '3. Horizontal Scale (100M-1B)', desc: '500+ chat servers behind LB with Proxy Protocol. Cassandra cluster (80 nodes, multi-DC) for messages. Kafka cluster (20 nodes) for async delivery. Redis for presence/sessions.' },
          { phase: '4. Global Scale (1B+)', desc: 'Active-active regions (US-East, EU-West, AP-South). Each region has local Cassandra + Kafka + Redis. Cross-region replication for user data. Anycast DNS routes to nearest region. Data localization per GDPR.' },
        ],
        distributedSystems: [
          { concept: 'CAP', decision: 'Messages: AP with at-least-once delivery. Dedup at receiver. Presence: AP (stale ok). Group ops: CP via Cassandra LWT (Paxos). User registration: CP (must be consistent globally).' },
          { concept: 'Idempotency', decision: 'Client-generated UUID v7 msg_id. Server Redis SETNX dedup (24h TTL). Retry-safe: same msg_id always produces same result. At-least-once delivery UI = effectively-once.' },
          { concept: 'Circuit Breaker', decision: 'Per-Cassandra-host circuit breaker. 60% failure ratio trips open (30s). Fallback: write to Kafka retry topic. Half-open: 100 requests before closing. Prevents cascade.' },
          { concept: 'Backpressure', decision: 'Credit-based flow control on WebSocket. Server grants N credits. Client stops at 0. Prevents: OOM from slow clients on 500K connections, TCP buffer bloat.' },
          { concept: 'Idempotency Key', decision: 'msg_id used as idempotency key end-to-end. Retry with same msg_id → already processed → dropped. Payment/group ops use separate idempotency tokens.' },
          { concept: 'SAGA', decision: 'Group creation: (1) create group (comp: delete) → (2) add admin (comp: remove) → (3) add members (comp: remove all) → (4) send notification (fire-and-forget). Step 3 fails → execute compensating txs.' },
          { concept: 'CQRS', decision: 'Write: messages_by_user (sender-outbox + receiver-inbox). Read: messages_by_conversation (chronological chat view). Separate read/write paths in Cassandra. No eventual consistency delay within same partition.' },
        ],
      },
      Instagram: {
        components: [
          { name: 'User Service', desc: 'Profiles, follow/unfollow relationships' },
          { name: 'Post Service', desc: 'Create/delete posts, metadata storage' },
          { name: 'Feed Service', desc: 'Generate personalized feed for each user' },
          { name: 'Like Service', desc: 'Track likes, count, like notifications' },
          { name: 'Comment Service', desc: 'Store comments, reply threads' },
          { name: 'Media Service', desc: 'Upload/resize photos, video transcoding' },
          { name: 'Search Service', desc: 'Search users, hashtags, locations' },
          { name: 'Cache Layer', desc: 'Cache feed, user profiles, trending posts' },
        ],
        flow: [
          '1. User uploads photo',
          '2. Media Service creates thumbnails (4 sizes)',
          '3. Post Service stores post metadata (caption, location, timestamp)',
          '4. Event published: PostCreated',
          '5. Feed Service updates followers\' feeds asynchronously',
          '6. Followers see post in feed within seconds',
          '7. User opens feed: served from cache (hot data)',
          '8. User likes post: Like Service increments counter, sends notification',
          '9. User comments: Comment Service stores with threading',
          '10. Real-time updates pushed via WebSocket to post author'
        ],
        challenges: [
          'Feed generation for 500M+ daily active users',
          'Handling 500K+ posts per second at peak',
          'Real-time notification delivery',
          'Duplicate media deduplication',
          'Hashtag indexing and trending calculation',
          'Comment thread consistency'
        ]
      },
      YouTube: {
        components: [
          { name: 'Upload Service', desc: 'Accept videos, initiate transcoding' },
          { name: 'Transcoding Service', desc: 'Convert video to multiple qualities (1080p, 720p, 480p, 360p)' },
          { name: 'Video Metadata Service', desc: 'Title, description, tags, categories' },
          { name: 'Search & Recommendation', desc: 'Full-text search, personalized recommendations' },
          { name: 'Streaming Service', desc: 'Serve video chunks at correct quality' },
          { name: 'Ad Service', desc: 'Select relevant ads, track impressions' },
          { name: 'Analytics Service', desc: 'Track views, watch time, engagement' },
          { name: 'Cache Layer', desc: 'CDN caches popular videos at edge' },
        ],
        flow: [
          '1. Creator uploads video (can be GB size)',
          '2. Upload Service stores in distributed storage (S3 equivalent)',
          '3. Transcoding job queued (could take hours for 4K)',
          '4. Multiple transcoders work in parallel (sharding by video size)',
          '5. Video indexed in Search Service',
          '6. Recommendations engine processes video features',
          '7. Viewer searches/browses, sees recommendations from cache',
          '8. Viewer clicks video, Streaming Service serves from CDN',
          '9. Adaptive bitrate: stream automatically adjusts quality based on bandwidth',
          '10. Ad Service selects most relevant ads (context + user history)',
          '11. Analytics Service tracks: views, watch time, retention, engagement'
        ],
        challenges: [
          '2B+ hours watched daily - need efficient storage/delivery',
          'Transcoding pipeline: 500 hours uploaded per minute',
          'Video search with 800M+ videos',
          'Personalized recommendations at scale',
          'Ad relevance and revenue optimization',
          'Content moderation at scale',
          'Live streaming with sub-5 second latency'
        ]
      }
    }
  }
};

function derive(prop) {
  const result = {};
  for (const t of TOPIC_DEFS) {
    for (const s of (t.subtopics || [])) {
      const key = `${t.id}:${s.name}`;
      if (prop === 'subtopic') {
        result[key] = s;
      } else if (prop === 'visualizer') {
        result[key] = s.visualizer;
      } else if (prop === 'route') {
        result[key] = s.route;
      } else if (prop === 'scenarioId') {
        result[key] = s.scenarioId;
      }
    }
  }
  return result;
}

const TOPIC_DETAIL = derive('subtopic');

export const TOPICS = TOPIC_DEFS.map(t => ({
  id: t.id,
  label: t.label,
  icon: t.icon,
  subtopics: t.subtopics.map(s => s.name),
}));

export const TOPIC_META = Object.fromEntries(
  TOPIC_DEFS.map(t => [t.id, t.meta])
);

export const VISUALIZER_MAP = derive('visualizer');

export const SUBTOPIC_ROUTES = derive('route');
export const SUBTOPIC_SCENARIO_ID = derive('scenarioId');

export const TOPIC_EXPLANATIONS = Object.fromEntries(
  TOPIC_DEFS
    .filter(t => TOPIC_DETAIL[`${t.id}:${(t.subtopics || [])[0]?.name}`] ||
                 CASESTUDIES[t.id])
    .map(t => {
      const subtopics = {};
      for (const s of (t.subtopics || [])) {
        const key = `${t.id}:${s.name}`;
        const detail = TOPIC_DETAIL[key];
        if (detail) {
          subtopics[s.name] = {
            explanation: detail.explanation,
            useCases: detail.useCases,
            realWorld: detail.realWorld,
            complexity: detail.complexity,
          };
          if (detail.tabs) {
            subtopics[s.name].tabs = detail.tabs;
          }
        }
      }
      const entry = { overview: t.meta.desc };
      if (Object.keys(subtopics).length > 0) entry.subtopics = subtopics;
      if (CASESTUDIES[t.id]) {
        entry.casestudies = CASESTUDIES[t.id].casestudies;
        entry.overview = CASESTUDIES[t.id].overview;
      }
      return [t.id, entry];
    })
);
