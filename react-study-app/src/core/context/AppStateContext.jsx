// Deprecated: Use UIContext (useUI) instead.
// Kept for backward compatibility — all consumers only used UI state.
export { UIProvider as AppStateProvider, useUI as useAppState } from './UIContext';
